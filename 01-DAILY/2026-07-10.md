---
date: 2026-07-10
weekday: Friday
tags:
  - daily
---

# 2026年07月10日 星期五

## meatloaf 工作日志（自动同步）

## 关卡难度分析 SQL (lv_name) 版补充 lv_id 排序键
- 文件：`D:\LibraryG\02-PROJECTS\TileMatch\打点\关卡数据报表_难度指标拓展版(lv_name).sql`
- 需求：同一 `lv_id` 可对应多个 `lv_name`；但「每个 lv_name 唯一对应一个 lv_id」成立 → 粒度保持 lv_name 一行。
- 改动：全链路 `lv_id` 重新带入，所有 `GROUP BY`/`JOIN` 改用复合键 `(lv_id, lv_name)`（避免单独用 lv_id 当唯一键导致 fan-out 或错误合并）。
- 输出新增 `Level_ID` 列，最终 `JOIN` 用 `ls.lv_id=ua.lv_id AND ls.lv_name=ua.lv_name`。
- 排序可切换：默认 `ORDER BY ls.lv_name`；备选注释行 `ORDER BY ls.lv_id, ls.lv_name`（同 lv_id 多行稳定）。
- 未覆盖原版 `关卡数据报表_难度指标拓展版.sql`（lv_id 序）。

## 复盘固化（此前）
- 查找项目文档/SQL/分析产出：一律先走 vault（Obsidian），绝不先按 summary 给的 `meatloaf_client01` 路径 Read（summary 路径可能过期，vault 才是单一源）。已写入 MEMORY.md 第 3 条铁律。
- conversation_history_summary 是自动生成的叙事回顾，权威度低于 MEMORY 约定与 vault 真文件，仅作背景线索、不作路径事实。
