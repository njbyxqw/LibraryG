---
date: 2026-07-30
weekday: Thursday
tags:
  - daily
  - TileMatch
  - TileScape
synced_from: automation/知识库同步与维护
---

# 2026-07-30 工作日志

## TileMatch

牌局生成逻辑变化检查。用户要求检查当前代码牌局生成逻辑是否有变化。确认 `14a62df458` 将 25% Tail 池从"不打乱（deterministic）"改为"轻量打乱（reserved, step=1/times=1）"。同步更新 vault 分析文档 + MEMORY。

### 检查范围
- 当前分支：`tile/tile_dev_level`
- 基线：上次分析的 `472d672680`（2026-07-20 22:26）
- 最新提交：`6838669cdf`（2026-07-29 18:23）
- 文件实际路径确认：`client/Assets/Game/TileV2/Scripts/GameCore/Logic/GameLogic/Module/LevelTileType/Strategy/AssignTileTypeByDepthStrategy.cs`（之前文档中写的 `Strategies/` 目录有误，实际是 `Module/LevelTileType/Strategy/`）

### 472d672680 之后 Logic 目录的 7 条提交

| 提交 | 日期 | 涉及牌局生成? | 说明 |
|------|------|:---:|------|
| `14a62df458` | 7/21 16:29 | ✅ | **25% 部分轻量打乱 参数可以配置** — 改 AssignTileTypeByDepthStrategy.cs (+21/-16) |
| `0bd2d32833` | 7/22 21:50 | ❌ | 障碍调控增加打点 — InLevelDDA.cs + 打点文件 |
| `cdba9cb760` | 7/22 17:22 | ❌ | 金牌不破碎 AB — GoldCanReceiveAddDamageCondition.cs |
| `2e63b0cc09` | 7/23 14:51 | ❌ | 障碍调控上报新增配置表 — InLevelDDA.cs + ObstacleRegulationReporter.cs |
| `ddeea0335a` | 7/23 15:44 | ❌ | 按批次统计障碍调控次数 — InLevelDDA.cs |
| `394e97015c` | 7/23 18:27 | ❌ | 障碍调控打点类型改 int — InLevelDDA.cs + 打点文件 |
| `32235b33f8` | 7/29 15:51 | ❌ | GC优化 — SpecialActions.cs |

### 14a62df458 核心变化（语义性变更）

**25% Tail 池从"不打乱（deterministic）"改为"轻量打乱（reserved）"**

1. 新增常量：`ReservedPoolRandomStep = 1`、`ReservedPoolRandomTimes = 1`
2. 注释更新：旧"不参与打乱" → 新"使用固定参数轻量打乱"
3. 变量重命名：`deterministicPool` → `reservedPool`（全文件）
4. 新增代码（非Trap + Trap 路径各一处）：`ShuffleTileType(reservedPool, ReservedPoolRandomStep, ReservedPoolRandomTimes)`
5. 效果：Tail 池做 step=1/times=1 的极轻量打乱，不再保持严格 A B C A B C 交替排列

### 同步完成（16:05）

用户确认后同步更新全部关联文档：

**vault `分析-AssignTileTypeByDepth分池打乱策略-v1.md`**：
- 概述：核心规则从"Tail 不打乱"→"Reserved 轻量打乱"；新增 14a62df458 更新说明
- 常量表：新增 `ReservedPoolRandomStep=1` / `ReservedPoolRandomTimes=1` 两行；ShufflePositionThreshold 含义更新
- ShufflePositionThreshold 详解：n2 注释从"Tail 不打乱"→"Reserved 轻量打乱"
- 深度映射表：分池列从"Tail 不打乱"→"Reserved 轻量打乱"
- 3.2 非Trap路径：流程伪代码 `deterministicPool`→`reservedPool`；新增 `ShuffleTileType(reservedPool, 1, 1)` 调用；效果说明更新
- 3.3 Trap路径：同步重命名 + 新增轻量打乱调用；新旧对比表"deterministic 不足"→"reserved 不足"
- 3.5 核心子方法：BuildColorPoolsInAssignmentOrder 输出说明更新
- 3.6 流程图二（非Trap）：C2/SLICE/G3/SHUFFLE/ASSIGN 节点全部更新
- 3.6 流程图三（Trap）：T_SLICE/T_SHUF/T_ASGN 节点全部更新
- 3.6 数据流速查表：②输出和③输入/输出更新
- 第四章提交历史：表追加 14a62df458 行；新增详细变更小节（5 维对比表+实际效果）

**MEMORY.md**：
- 分池规则验证：从"Tail 不打乱"→"Reserved 轻量打乱（step=1/times=1）"
- 提交历史表：追加 `14a62df458` 行

---

## TileScape

牌局生成逻辑一致性检查。对比 TileScape 项目与 vault 文档 `分析-AssignTileTypeByDepth分池打乱策略-v1` + `分析-障碍Tile生成与序列逻辑-v3`，**核心逻辑 100% 一致**，TileScape 有少量后续扩展（BatchBinding、Effect刷新、Bot逻辑等）。

### 逐项对比
- AssignTileTypeByDepthStrategy 全部常量和核心方法逐方法吻合
- Board.InitializeFromLevelConfig 逐行一致
- AssignTileTypeStrategy 备用策略完全一致

### 差异
- TileScape 的 InitConfig 多了 `ApplyBatchBindingStrategy`、`LoadEffectConfigsForLevel` 等步骤
- 这些是 TileScape 独立扩展的功能，不影响核心牌局生成逻辑
