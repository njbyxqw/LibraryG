---
date: 2026-07-20
weekday: Monday
tags:
  - daily
synced_from: meatloaf_client01/.workbuddy/memory/2026-07-20.md
---

# 2026-07-20 工作日志

## 脚本工具盘点 + 整理
- 盘点 `local_py_script/` 全部脚本：generate_tile_levels / inject_diff_step_time / level_tracker / sync_daily_logs / 3个HTML工具
- HTML 去重：`关卡数据对比工具.html` ≡ `_v4_双因子.html`（MD5一致），保留前者
- level_tracker 报告归并：保留 `level_tracker_report_latest.html`，3个带日期副本归档
- generate_tile_levels.py：API Key 硬编码 → 环境变量（OPENAI_API_KEY/DEEPSEEK_API_KEY）

## 工作空间污染诊断与清理
- **根因**：CloudStudio 部署时间戳目录 + WorkBuddy 自动化临时目录 + 脚本硬编码 OUTPUT_DIR
- **清理**：删除 3 个 `2026-*` 时间戳目录 + `.cloudstudio_tmp/`，归档 aihot-daily HTML → vault 工具/
- **迁移**：`lvname_grouped/` → `local_py_script/lvname_grouped/`，修正 OUTPUT_DIR 为 `os.path.dirname(__file__)`
- **残留**：`2026-06-29-11-45-23/` 空壳被锁，需手动删

## 关卡工具集成台（方案② → 真机失败 → 回退）
- 新建 `tools_hub/tools_launcher.py`（stdlib HTTP :8777）+ `start_tools_hub.bat`（双击启动）
- 真机失败：窗口闪退 + ERR_EMPTY_RESPONSE（疑似残留进程占端口 + .bat 无 pause）
- 用户决策：暂停集成台，回到原脚本工具

## 关卡数据对比工具 v5
- 修复文件上传无反应（删除重复 DOM 块）+ 模块宽度对齐
- **性能优化**（6个瓶颈）：innerHTML 循环拼接 → 一次性赋值；indexOf → Set O(1)；嵌套 find → 预建索引 Map；5次filter → 1次forEach分类
- 布局放宽 + 默认值调整（人均消耗下限200→0，权重流失率40→60）

## 花色打乱逻辑修复（AssignTileTypeByDepthStrategy）
- **三个bug**：slot索引切分按JSON顺序非深度排名；8a42d60c9f引入Shuffle对象搞反；池内容搞反
- **修复**：非Trap路径 — 按depth升序切shallow/deep/sequence + Shuffle(shufflePool)；Trap路径同步修正
- **Gizmos**：新建 `TilePoolDebugGizmo.cs`（Scene视图色块覆盖：蓝=Tail/紫=Trap/橙=SEQ/灰=固定）+ Console聚合统计
- 编译修复：TileEntry.tileIndex 不存在 → .Position；命名空间 Script.Debug 冲突 → Script.TileDebug；FB SDK 平台宏保护

## 知识库归档：牌局生成深度显示
- 新建 vault `编辑器/工具-牌局生成深度显示-v1.md`（cat_order 050）
- 更新 vault `游戏逻辑/其他/分析-障碍Tile生成与序列逻辑-v1.md`（V2分池重写）
- 更新 _MOC.md + 顺序索引

## 深度命名反转 → 纠正
- 误判：代码"深浅"与视觉相反（世界Z反转主导）
- **纠正**：Sorting Order (BoardView.cs:126 `pos.z * 160`) 才主导层叠；代码深浅与视觉**一致**
- depth最小25%（视觉最上层）→ Tail不打乱 ✅
- 同步纠正 vault 三处文档 + MEMORY.md

## 远端提交分析（5885672785 + baca100318 + 35383e5c6a + 472d672680）
- 5885672785：两池合计Shuffle一次，去除序列/75%各自整除判定
- baca100318：Fisher-Yates → ConfiguredRandom步进Shuffle
- 35383e5c6a：修复花色聚集（AAA BBB CCC → ABC ABC）
- 472d672680：最大重构 — 预排序+位置直通，删除3旧方法新增3新方法

## 新建独立分池策略文档 + 重写障碍Tile v3
- 新建 vault `游戏逻辑/其他/分析-AssignTileTypeByDepth分池打乱策略-v1.md`（完整算法流程+提交历史+代码引用）
- 重写 `分析-障碍Tile生成与序列逻辑-v1` 为 v3（10章节+代码索引，约500行，16个.cs文件全覆盖）
- 精简旧工具文档和分析文档（深度映射/提交历史 → 引用新文档）
