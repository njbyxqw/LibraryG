---
date: 2026-07-06
weekday: Sunday
tags:
  - daily
---

# 2026年07月06日 星期日

## meatloaf 工作日志（自动同步）

### 一、今日工作记录

#### 1. 关卡难度分析 SQL — campaign 过滤口径纠正（用户澄清）
- **纠正**：真实原版 `level_stats` 中 campaign 过滤行**是注释掉的**（含全部投放计划）。上周用户给的"基准"忘了注释，导致误判。
- **2000+ 差异根因**：原版（真实，campaign 注释=全计划） vs 我存的拓展版（base_events 里 campaign 写成 ACTIVE=只算 2 个计划）→ 拓展版首关用户数比原版**少 2000+**。
- **修复**：新建 `D:\meatloaf_client01\SQL_关卡难度分析_原版.sql`（campaign 注释正本）；改写 `SQL_关卡难度分析_拓展版.sql`（base_events campaign 改注释、AB 对齐 `73_4` 注释）。现两版 `level_stats` 人群口径一致。
- **一致性规则**：原版/拓展版 campaign 必须同为注释；切单计划须两版同步取消注释；base_events 仅共享扫描，不擅加过滤。

#### 2. 文档同步 — 完整 SQL 入档
- 两版完整可执行 SQL 补入 `打点/报告-关卡难度分析SQL_完整版_2026-07-03.md`（新增"七、完整 SQL 代码"原版+拓展版）。
- 修正：拓展指标表 E1~E15 → 真实 14 列 E1~E14；CTE 图"拓展15列"→"14列"；补"八、版本历史"。
- MEMORY.md 索引"拓展11列"→"拓展14列"已修正。

#### 3. Obsidian 确立为优先知识库（体系搭建）
- 项目 + 用户级 MEMORY 新增「Obsidian 优先知识库管理规则」（5 原则 + MCP 优先级表 + 健康标准）。
- 创建 `规范-知识库健康检查.md`（6 项检查 / 4 级评分 / 修复指南）。
- 首次健康检查：**92.3% 🟡** → 修复 9 断链 + 10 frontmatter 缺失 → **98.3% 🟢**。
- `_MOC.md` 加 Dataview 仪表盘（4 个 DQL：全文档/分类计数/最近更新/草稿）。
- Notion 降级：Obsidian 为单一源，Notion 仅作可选云镜像（见待办 T3）。

#### 4. Git / 提交硬规则范围纠正（用户纠正）
- 「禁止自动 commit」**仅针对 `meatloaf_client01` 公用 git**；**私人 git 在用户授权下可 commit**。
- 撤销上一轮错误建议"把 `.workbuddy/memory` 从 gitignore 移除并 commit 到 gitlab"（违反本规则，作废）。

#### 5. Notion 单向云镜像方案评估
- **方向**：Obsidian（单一源）→ 单向同步 → Notion（云镜像 / 离机副本）。用户熟悉 Notion 且需离机安全；私人 git 暂缓（用户需时间学 git）。
- **阻塞**：Notion connector 当前 **disconnected**，无 notion MCP 工具，无法直接推送。
- **方案对比**（52 篇 .md，最大 7514 行）：
  - 甲 MCP 手动：读 52 篇进上下文 + 逐块调 → 输入 token ≈ 60k–100k + 数百次工具调用，慢，大文件有 block 数风险
  - 乙 Python 脚本：写一次脚本 + 跑一次 → LLM token ≈ 3k–5k，自动分批 append（≤100 block/次），快且稳；**写 Notion 不碰本地文件，不违反"禁止脚本改本地文件"规则**（仍须用户授权）
  - 推荐：乙
- **token/积分汇率**：未知（平台内部计费），已向用户说明去客户端用量页查；本次同步成本按方案乙估算很低。

### 二、待办清单（知识库管理与维护 · 持续任务）

> 持续任务引擎：周度自动化 `automation-1783324340942`（每周一 09:00），执行健康检查 + 复核本清单 + 输出报告。

| # | 事项 | 状态 | 依赖 / 备注 |
|---|------|------|------------|
| T1 | 首次健康检查修复（9 断链 + 10 frontmatter） | ✅ 已完成 | 92.3% → 98.3% 🟢 |
| T2 | 周度健康检查自动化 | ✅ 运行中 | automation-1783324340942，每周一 09:00 |
| T3 | Obsidian → Notion 单向首次同步 | ⏸ 待启动 | 阻塞：需重连 connector 或给 token+父页面；甲/乙方案未定。**用户当前倾向先不做，待学 git** |
| T4 | 6 个命名不规范文件加前缀 | ⏸ 暂缓 | 用户决定暂不改（内容无误即可）；后续内容大改时一并处理，需同步改 wikilink |
| T5 | Obsidian 安装 Dataview 插件 | ⏸ 待用户 | 仪表盘 DQL 已写好，装完即渲染 |
| T6 | 知识库离机备份最终落地 | ⏸ 待定 | 方向已定=Obsidian→Notion 镜像；私人 git 暂缓（用户学 git 中），授权后可 commit |
| T7 | 标准笔记模板(Templater) + 标签受控词表 | ⏸ 待办 | 预防 future frontmatter/标签漂移，低成本高价值，下次顺手做 |

### 三、关键约定（本日新立，跨会话有效）

#### 6. 拓展版 SQL 语法报错排查（16:59）
- **报错**：`line 7:30: mismatched input ';'`（Trino 解析错误）。
- **诊断**：盘上文件结构合法，报错"line 7"与文件不符 → 用户运行的是含中途 `;` 的残留版本。
- **修复**：去掉结尾 `;`（TA SQL 框不需要），零游离分号。
- **提醒**：核对运行的文本与文件是否一致（CTE 之间应为逗号非分号）。

#### 7. 拓展版 vs 原版数量偏差最终修复 + 存档（17:14~17:38）
- **根因**：之前尝试去掉 `JOIN v_user_48` 反而破坏了与原版的等价性；原版有 JOIN，拓展版没有 → `level_stats` 人群集不同。
- **最终修复**：`base_events` 恢复原版的 `FROM v_event_48 e JOIN v_user_48 u ON #account_id`，全文仅此一处 FROM+JOIN。
- **验证**：数量完全对上（用户确认"完美"）。
- **完整逻辑审查**：4 个 CTE + 最终 SELECT 逐列审查通过，质量标签 5 路径覆盖完整，无 Bug；仅 `revive_var_user` 计算范围是全量用户（非仅通关），非缺陷。
- **正式命名存档**：
  - `关卡数据报表_原版.sql`：单 CTE `level_stats`，36 列，用户权威正本
  - `关卡数据报表_难度指标拓展版.sql`：`base_events` + 4 CTE，原版 36 列 + 拓展 14 列
  - 旧文件 `SQL_关卡难度分析_原版.sql` / `SQL_关卡难度分析_拓展版.sql` 保留作开发历程
1. **禁止自动 commit**：仅限 `meatloaf_client01` 公用 git；私人 git 在用户显式授权下可 commit（先展示待 commit 内容）。
2. **Obsidian = 唯一权威知识库（单一源）**；Notion 仅云镜像，不在 Notion 反向编辑。
3. **写 Notion 不碰本地文件**，不违反"禁止脚本改本地文件"规则。
4. 文档只生成到 `D:\LibraryG\02-PROJECTS\TileMatch\`；生成后同步更新 `_MOC.md` 与 MEMORY.md 索引。
