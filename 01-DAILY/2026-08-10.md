---
date: 2026-08-10
weekday: Monday
tags:
  - daily
  - TileScape
synced_from: automation/知识库同步与维护
---

# 2026-08-10 工作日志

## TileScape

TMP 字体替换参考文档整理 + 手套拿牌（MoveToOverBar）功能表现层完整梳理，并完成"选中牌提到上层后停留 0.1s 再飞弃牌区"的需求对齐补丁。

### TMP 字体替换参考文档

- 创建 `Docs/TMP_Font_Replacement_Guide.md`
- 整理了项目 TMP 字体体系完整结构（源字体 / TMP 资产 / 字符集 / 材质 / Fallback / FontArt / 批量工具）
- 梳理两种字体替换方式：**同名替换**（保留 GUID）和**新名字替换**（新 GUID + 改引用）
- 含步骤、对比表、常见问题排查、快速流程图
- 关键文件：
  - 源字体: `Assets/Betta/Res/Fonts/`（6 个 TTF/OTF）
  - TMP 资产: `Assets/Betta/Resources/Fonts/{lang}/{lang}.asset`（11 语言 + Fallback）
  - 字符集: `Assets/Betta/Res/Fonts/I18n/{lang}.txt`
  - 批量工具: `Assets/Module/Editor/TMPFontBatchTool.cs`（菜单 Tools > TMP Font/）

### 手套拿牌功能表现层梳理

- 分支: `feat-new_movetooverbar_prop`，4 条提交（`3b09e8bbb` → `ff53defc7`）
- 表现层改动分 4 层：
  1. **UI 表现层 (UIGamePanel)**: PropPresentation(+345 行核心)、PropUseFlow(+118 行)、PropButton(+2)、PropController(+8)
     - 飞行图标动画（DOTween DOPath 弧线）+ MoveToOverBarTilePresentationScope（选中牌三态快照）
     - 回调签名 `Action` → `Func<bool>`（表现层需知道执行结果决定提交/回滚）
  2. **View 表现层 (GameView + Headless 双实现)**: ViewController(+17×2)、BoardView(+11×2)、OverBarCellView(×2)、IController(+2)
     - RefreshEffectBinding / KeepDetachedEffectOnBoard / OverBar 不受 `_maxStack` 限制
  3. **翻转牌 Bug 修复**: FlipTileView(+27) / JokerFlipTileView(+27) — 手套拿走序列牌后剩余牌恢复缩放
  4. **配置层**: 25 个 JSON 新增 MoveToOverBarUse 事件引用 + 8 个 ShellBox 配置
- 总改动: ~2535 行新增 / 152 行删除
- 对应知识库文档：[[道具系统/手套道具MoveToOverBar表现层分析|手套道具 MoveToOverBar 表现层分析]]

#### 补 0.1s 停留（选中牌提上层后停留再飞弃牌区）

- 需求对齐：飞行图标砸到目标牌消失 → 选中牌提到最上层 → **停留 0.1s** → 牌飞到弃牌区
- 之前缺停留：OnComplete 直接调 `Complete()`，选中牌提到上层后立即执行逻辑层
- 改动文件：`TileGamePanelPropPresentation.cs`（5 处，+8 行）
  1. Config 新增 `SelectedTileHoldDuration = 0.1f`
  2. 字段新增 `_presentationApplied` flag
  3. OnComplete: 销毁飞行图标 → Apply → DelayedCall(0.1s) → Complete
  4. Complete: Apply 条件加 `!_presentationApplied` 跳过重复（避免闪烁）
  5. Cancel: 重置 flag
- 3 张牌排序现状: 所有牌 sortingOrder=100，彼此无相对差异（icon=299 / bg=298 / effects=300+）
