---
date: 2026-07-03
weekday: Thursday
tags:
  - daily
---

# 2026年07月03日 星期四

## meatloaf 工作日志（自动同步）

### 关卡难度分析 SQL — 完整开发与归档

#### 阶段一：原版 SQL 审查
对用户提供的 TA 关卡难度分析 SQL（单 CTE `level_stats`，36 列 + 27 衍生指标）进行代码审查，结合 C# 源码验证打点字段上报行为。

核心发现（4 项 P0/P1 问题，已标记暂缓）：
- P0: 保底胜利(levelend=6)被遗漏 — LevelFinishReason 枚举含 GuaranteeSuccess=6，但只判断 levelend=1
- P0: lv_id 字符串排序 — TA 存 VARCHAR，ORDER BY 产生字典序
- P1: 失败平均用时分母 — 用 start_count - finish_count 含退出/闪退
- P1: 首次失败剩余率分母 — 分子子集限定但分母全量 card_num

关键源码文件：`LevelFinishReason.cs`, `LevelControlData.cs`, `LevelFlowData.cs`

#### 阶段二：质量标签归因逻辑分析
分析 SQL 末尾的 CASE WHEN 归因逻辑（trim_ratio + 分位值比率区分"少数极端用户"vs"关卡波动"）。

发现 2 处覆盖盲区（6 路径中 4 条正确、2 条漏判），改进建议：
- trim 阈值 0.15→0.10 消除灰色地带
- p99/p90 阈值 2.5→2.0 降低漏检率
- 新增⚡中间标签覆盖"有异常值但无尾部断崖"

#### 阶段三：拓展版 SQL 生成
基于原版 `level_stats` 做最小改动拓展：
- 原版 36 列 **一字未改**，注释全部保留
- 新增 3 个 CTE：user_lv_base（用户-关卡粒度）、user_lv_quantile（分位值）、user_lv_agg（用户级聚合+质量标签）
- 最终 SELECT 追加 15 列拓展指标（E1~E15）：方差/标准差/分位数/trim均值/质量标签/N>3 比率
- 质量标签采用改进版 6 路径全覆盖
- 补全 9 处 NULLIF

#### 阶段四：Bug 修复
1. **列歧义**：user_lv_base 中 `#user_id` / `lv_id` ambiguous → 加前缀修复
2. **数据截断**（关键修复）：level_stats 和 user_lv_base 各自独立扫描 `v_event_48 JOIN v_user_48`，TA 平台资源耗尽静默截断 → 新增 `base_events` 共享 CTE，一次扫描承接所有 JOIN+过滤，后续 CTE 全部 `FROM base_events`

#### 阶段五：原版基准确认
用户提供单 CTE SQL 作为唯一原版基准，拓展版数据量必须与此一致。

确认项：level_stats 的 36 个 SELECT 表达式逐字相同；base_events 的 JOIN+WHERE 与原版一致；改为 FROM base_events 后数据不丢失。

#### 输出文件
- 生成 `D:\meatloaf_client01\SQL_关卡难度分析_拓展版.sql` — 可执行 SQL 文件
- 生成 `报告-关卡难度分析SQL_完整版_2026-07-03.md` → Obsidian `02-PROJECTS/TileMatch/打点/`
- 更新 `MEMORY.md` 新增「SQL 关卡难度分析 — 原版基准」节

#### 暂缓问题
| 问题 | 原因 |
|---|---|
| levelend=6 遗漏 | 人数极少，暂不需要 |
| lv_id 字典序排序 | 待用户独立验证后再决定是否修复 |
| 失败用时分母 | levelend=2 占绝大多数，实际偏差小 |
| 首次失败剩余率分母 | 待后续验证 |

### 编辑器统计功能分析

分析编辑器中 StatisticView 和 ExportCSV 的区别，确认 ExportCSV 可新增 LevelFileName 列（改动约 3 行，数据模型已有 `LevelConfigData.LevelName`）。

生成 `编辑器统计功能分析.md` → Obsidian `02-PROJECTS/TileMatch/编辑器/`。

用户表示功能之后处理，本次仅归档分析文档。

## 知识库索引补全（B方案）

发现 MEMORY.md 索引覆盖率仅 25%（12/48 篇笔记）。按"高频入口 + 搜索兜底"的 B 方案补全：

- **概念快速索引**新增 11 条：Effect牌/障碍牌MOC入口、类型全览、Shuffle改造、关卡数据分析工具、关卡文件管理/追踪、跑关机器人、文档分类规范
- **目录表**新增 3 个分区：
  - 工具(orange) — 3 篇（数据分析工具、文件追踪、文件管理）
  - 障碍牌(green) — 8 篇（类型全览、Rocket、CandyCube、Ore、ShellBox、容器、翻转、特殊机制）
  - Effect牌(green) — 16 篇（类型全览 + 15 种独立笔记）
- 修复 Rocket 区重复条目（报告-关卡难度分析SQL）

**检索机制确认**：用户问"火山牌" → 概念索引命中"障碍牌类型全览" → 看到 Volcano TileType=5080 → 发现无独立笔记 → Grep 代码找 Behaviours/Volcano

### 知识库文件恢复（Python 脚本污染事故）

#### 事故原因
批量给知识库文档添加 `## 关联` 章节时，Python 脚本 bug 将 `规范-本地扩展开发.md` 的内容错误覆盖到其他文件。

#### 污染扫描
- Grep 搜索 `^title: 本地扩展开发规范` 定位所有被污染文件
- 实际发现 12 个被污染文件（背景文档预估 18 个，其中 6 个为误报）

#### 恢复方式
| 恢复方式 | 文件数 | 说明 |
|---------|--------|------|
| 工作空间产物直接复制 | 2 | RocketVL存档、关卡文件变更追踪报告 |
| 从SQL审查报告+拓展版SQL重建 | 2 | 关卡难度分析SQL完整版、打点SQL参考 |
| 从日报+MEMORY索引+代码库重建 | 8 | 道具逻辑、Shuffle、编辑器统计、gitignore、文件追踪/管理工具、数据分析工具 |

#### 框架文件补充
- `局内障碍知识库_MOC.md` — 补充完整 Effect 类型表（16种）+ Effect 文档索引（16篇）+ 障碍牌文档索引（8篇）
- `分析-关卡编辑器界面与功能逻辑梳理-v1.md` — 验证内容完整（340行，13章节）
- `分析-Tile打点解析-v1.md` — 验证内容完整（175行，5章节）

#### 验证结果
- 恢复后 Grep 扫描：**零污染文件** ✅
- 背景文档状态已更新：所有❌/⚠️ → ✅
- `_MOC.md` 索引完整，包含所有恢复文件

#### 输出
- 更新 `D:/LibraryG/00-INBOX/知识库文件恢复背景_2026-07-03.md` — 状态改为"✅ 恢复完成"
- 12 个被污染文件已全部恢复到 `D:/LibraryG/02-PROJECTS/TileMatch/` 对应目录
