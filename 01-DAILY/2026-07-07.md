---
date: 2026-07-07
weekday: Tuesday
tags:
  - daily
---

# 2026年07月07日 星期二

## meatloaf 工作日志（自动同步）

## BlockBg 底板选择器（v1 → v2 → v3 迭代）

### v1（已废弃）— LevelConfigView 面板下拉
- 放错位置，用户纠正

### v2（已废弃）— TileSelectionView 分类 + 映射文件 + PlayMode 重试
- 右侧 Block_bg 分类栏（与 Tile/Element/TileType 并列），80x80 预览网格
- 操作：选底板 → 点棋盘牌块 → 写 `.blockbg.json` 映射文件 → 反射更新 SpriteRenderer
- **失败原因**：
  1. `SelectGridPos` 事件只在 Effect 点击和 OnSelectTile 时 dispatch（实测可用）
  2. Play 模式位置匹配不准：`transform.localPosition` ≠ 棋盘坐标（有 Scale/Offset）
  3. Play 模式时序：tile 可能在 `EnteredPlayMode` 后才创建，需 `EditorApplication.update` 持续扫描
  4. **根本问题**：数据不在关卡 JSON 里，Play 模式需要额外 hook 覆盖，脆弱且复杂

### v3（已废弃）— 关卡数据模型加 BlockBg 字段
- 未知同事已在共享分支提交了完整实现，冲突且冗余

### ✅ 最终方案 — 使用同事的 "牌底笔刷" commit (0f0b335519)
- 宫丁峰 12:04 提交，修改 27 个文件，1065+ 行
- 核心设计：
  - 数据：`LevelTileConfig.BgId` (int, 0=默认) → `TileData.BgId`
  - UI：`TileSelectionBg` 组件 + `TileBG` 分类（内置，非本地扩展）
  - 棋盘：`BoardBlock.OnTileBtnClick` 加 `else if (CurrentTileBg > 0)` 分支
  - 命令：`BgIdModification` + `ModifyTileBgCommand`，支持 Undo/Redo
  - 运行时：`TileView.ApplyTileBg()` 直接覆写 `background.sprite`
  - 保存：`LevelConfigWriter` 中 `BgId != 0` 时写 JSON
  - 资源：`block_bg/block_bg{N}.png` (编号 3~8)

### 🔑 核心教训
1. **动手前先看分支**：同事已经做了同样的功能，我们白写了 3 个版本
2. **本地扩展的边界**：数据模型变更（关卡 JSON 加字段）不走本地扩展——数据流不闭环。改源码是正确选择
3. **以后先确认**：功能是否已有人做、是否要提交 git、是否涉及数据模型变更

## SQL 关卡难度报表 — v3 迭代

### 质量标签阈值优化（11:53~12:04）
- **问题**：「关卡自身波动大，需剔除」命中率 53.6%，太多
- **业务背景**：用户确认「难度 >5 体验差」= 用户通过一关的开始次数 >5
- **p50 分布诊断**：95.3% 关卡 p50=1（过半玩家一次通关）
- **修复**：`p90/p50 > 2.0` → `p90 > 5 AND p90/p50 > 2.0`（双重过滤，命中率 53.6% → 9.3%）
- **文件**：`关卡数据报表_难度指标拓展版.sql`、`SQL_关卡难度分析_拓展版.sql`

### 新增未通关 P90 列（14:14~14:20）
- 新增 `p90_attempt_quit`：90% 的放弃者尝试了多少次以内就放弃
- 涉及改动：`user_lv_quantile` +1 行、`user_lv_agg` +2 行、最终 SELECT +1 列
- 同步两个文件（正式版 + 开发版）

## 局内道具逻辑分析 & Shuffle 改造（14:40~17:06）

### 局内道具深入分析
- 深入分析 `局内道具逻辑梳理.md`，覆盖 InGame/PreGame 全部 7 种道具的源码链路
- **Shuffle 核心发现**：不是纯随机——将 Bar 需要的同花色牌交换到高亮层；但只优化 `bar.LastUnmatchedTile` 的 1 种花色
- 标记 RevertLastAdd 为弃用

### Shuffle 改造输出
- **生成** `Shuffle改造AB测试方案.md` — 对照组（一直帮）vs 测试组（概率梯度+保底），含指标/埋点/实验周期/决策标准
- **生成** `风车Shuffle优化提需.md` — 策划视角纯逻辑提需，八节零代码
  - 核心：从「只帮 1 种花色」→「帮 Bar 中所有凑不齐的花色」，按 needCount ASC 排序轮转分配配额
  - 测试版：概率梯度（剩余 5/4/3/2 槽四档）+ 保底（每次 +20%），Bar 压力大必帮、压力小保留随机感
  - 不动：交换算法/Effect 处理/UI 动画/DDA 通知全部复用
