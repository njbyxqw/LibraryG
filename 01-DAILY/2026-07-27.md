---
date: 2026-07-27
weekday: Monday
tags:
  - daily
synced_from: automation/知识库同步与维护
---

# 2026-07-27 工作日志

## Magnet 开发收尾：流程梳理 + 踩坑文档
- 将 Magnet 开发全流程整理成规范文档，存档 Obsidian：[[规范-新障碍开发流程与踩坑记录]]
- 覆盖：设计对齐 → 代码层新建/修改清单 → ECA 事件链路 → 编辑器三层配置体系 → 快速验证清单
- 关键踩坑纪要：
  - Effect 不能订阅 TileClicked，必须用框架的 EffectClick 机制 (HandleClick=true)
  - .json~ 是源文件，.bytes 是 protobuf 编译产物，改了源必须重新编译才生效
  - 所有 Effect 必须有 "Life" 字段，否则初始化崩溃
  - Action 中的 TileGroup 用 tile.TileType.GetTileGroup()，Visibility 用 Board.GetAllTileListByVisibility()
  - BezierUtil 接收 List<Vector2> 不是 List<Vector3>
  - ActionResult 的命名空间在 Action 层，不在 Implementation 子目录

## 编辑器白盒加载 - 两级目录可配置化
- 分析编辑器"从模板加载"（白盒）功能，确认路径硬编码在 `EditorConfig.cs` 的 `LevelLoadTemplateRootGroupName="LevelBox"` / `LevelLoadTemplateLevelGroupName="TBD"`
- **代码改动**：
  - `LevelLoadFromTemplateView.cs`：新增两级 Dropdown（RootDir/GroupDir）替代硬编码；选中值通过 PlayerPrefs 持久化
  - `EditorConfig.cs`：新增 2 个 PlayerPrefsKey + 2 个默认 ID=1 常量；保留原 name 常量
- **Prefab 更新**：生成 Editor 脚本 `Assets/Editor/LocalExtensions/AddTemplateDropdownNodes.cs`，在 Unity 中执行菜单 `Tools → Add Template Dropdown Nodes` 即可自动添加 RootDir 和 GroupDir 节点
