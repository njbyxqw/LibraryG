---
date: 2026-07-09
weekday: Thursday
tags:
  - daily
---

# 2026年07月09日 星期四

## meatloaf 工作日志（自动同步）

## 工作空间产物归属与健康检查体系建立

### 背景
用户反馈工作空间根目录出现多个不明来源目录（`2026-06-29-11-45-23/`、`Claw/` 等），git status 显示大量非 client 的 untracked 文件。需建立预防规则和定期检查机制。

### 溯源
- `2026-06-29-11-45-23/`：WorkBuddy 自动化临时工作空间，搭载关卡数据分析 HTML 工具开发（含自身 .workbuddy/memory/），因 cwds 指向项目根导致生成在根目录
- `Claw/`：某次 WorkBuddy 会话产物输出到了错误路径，含一个工作日志文件

### 已执行动作
1. **同步到 vault**：`火箭牌V2当前逻辑分析（基于代码）-v1.md` → Obsidian Rocket 目录
2. **同步到 vault**：`RocketNewBranch/` 14 个源码文件 → Obsidian Rocket/RocketNewBranch/
3. **删除**：`Claw/`（日志已在 daily log 中覆盖）
4. **删除**：`关卡数据报表_难度指标拓展版.sql`（与 SQL_拓展版 完全重复）
5. **删除**：`RocketVL-闪电球视觉替换.md` 短版（完整版已在 vault）
6. **清空**：7 个空自动化/时间戳目录
7. **Git exclude**：追加 `2026-*/` 模式，防止时间戳目录再出现

### 规则建立
- MEMORY.md 新增「工作空间产出归属规范」：
  - 产出类型 → 归属对照表（7 种类型 + 生成位置 + 归档目标 + 同步方式）
  - 异常检测规则（新会话触发：git status 扫描非 exclude 的 untracked 项）
  - 长期遗忘检测规则（对比工作空间文件与 MEMORY.md 索引）

---

## 第二阶段：彻底清查非 client 内容（第2轮）

### 清理清单

| 项目 | 操作 | 状态 |
|---|---|---|
| **local_py_script/ 清理** | | |
| 5 个 HTML 最终报告 | 复制到 vault `工具/关卡文件追踪/` 后删除 | ✅ |
| 3 个 test HTML | 直接删除 | ✅ |
| `level_tracker_template.html` | 模板文件，删除 | ✅ |
| 7 个 .md 中间版本报告 | 全部删除（最终版已在 vault） | ✅ |
| `__pycache__/` | 删除 | ✅ |
| `tile_levels_output/` | 删除 | ✅ |
| `level_tracker.db` | 复制到 vault `工具/关卡文件追踪/`，本地保留（工具依赖） | ✅ |
| **SQL 文件**（4 个） | 已确认 vault 有同步副本，删除 | ✅ |
| **workbuddy_archives/** | 整体删除（全部内容已入库 vault） | ✅ |
| **Claw/** | 空壳，删除 | ✅ |

### 保留项
- `local_py_script/` → 仅留 6 个 .py + .db（1.5MB），其余全清
- `.workbuddy/reports/` → 用户要求留着
- `2026-06-29-11-45-23/` → 已溯源，暂不处理

### 最终 git status
- **受 client/ 外的 untracked 文件：0** ✅ 完全干净

### 未完结待办
- 工作空间健康检查自动化 → 方案暂缓

---

## 关卡难度分析 SQL：新增 lv_name 排序版本

### 背景
用户贴了一版按「关卡文件名(lv_name)」分组/排序的 SQL，要求把 V3 拓展版改成这种形式。

### Obsidian MCP 排查
- 任务开始报 `fetch failed`，定位为**冷启动时序**（Obsidian Local REST API 端口 27123 当时未监听），非配置错误。
- 验证：mcp.json 配置正确；obsidian-mcp 可执行存在；端口/鉴权 curl 正常；复测 `search_vault` 命中 5 个笔记 → MCP 已恢复。
- 提示：再遇 `fetch failed` 隔几秒重试即可。

### 改动（另起新文件，不覆盖原版）
- 新文件：`D:\LibraryG\02-PROJECTS\TileMatch\打点\关卡数据报表_难度指标拓展版(lv_name).sql`
- 全部 `lv_id` → `lv_name`：base_events 选取、level_stats/group、user_lv_base/group、user_lv_quantile/group、user_lv_agg/group+join、最终 Level 列、最终 join ON、ORDER BY
- 用户明确**不算关间/总流失率** → 删掉 LAG 窗口，两列改回 `ROUND(9999,2)` 占位（同用户贴版）
- 原版 `关卡数据报表_难度指标拓展版.sql`（lv_id 序）保留不变
- SQL 文件均已在 vault，本地根目录无副本（符合产出归属规范）
