---
date: 2026-06-24
weekday: Tuesday
tags:
  - daily
---

# 2026年06月24日 星期二

## 火箭牌V2重构代码分析

### 提交
- 最近提交：`e83f832fe7` — 火箭牌重构 (2026-06-24 10:58)
- 44 个文件变更，2655 insertions，198 deletions

### 生成文档
- 生成 `workbuddy_archives/RocketCode/RocketV2-完整逻辑（重构版）.md`
- 覆盖三条主线：生成规则（RocketDepthStrategy）、Lucky Rocket概率、Chain Rocket链式攻击

### 主要发现
- 生成策略从 `RocketNormalStrategy` 改为 `RocketDepthStrategy`
- `PrepareChainedAttackAction` 替代旧的 `PrepareAttack`，逻辑层预计算所有波次
- `RocketPrimaryTargetFilter` 让主波 Rocket 与普通牌同权重
- `RocketChainTargetFilter` 链式波优先普通牌，不足兜底火箭
- Lucky 概率通过 `LevelRocketData.GetRocketLuckyProbability()` → DataBridge → Action 传递
- 生成数量 base=3（不是文档说的 4），>28 时每多 5 组 +1

## 详细技术文档生成

### 生成文档
- 生成 `workbuddy_archives/RocketCode/RocketV2-技术实现详解.md`

### 文档特点
- 参照资料库中的火箭牌V2文档（`RocketV2-逻辑梳理_定稿.md`、`RocketV2-完整逻辑.md`、`RocketModify.md`）
- 提供代码级详细分析，包含：
  - 完整代码片段（带行号引用）
  - 详细数据流图（文本格式）
  - 边界情况与异常处理
  - 配置常量与参数表
  - 测试验收清单（10大类、50+子项）
- 覆盖三条主线：
  1. 生成规则优化（RocketDepthStrategy深度分桶、连胜动态调节、Highlight兜底）
  2. Lucky Rocket（概率映射表、数据流贯通、录像兼容）
  3. Chain Rocket（触发条件、波次规则、目标筛选、事件抑制）

## 牌局生成全规则梳理

### 分析范围
- 阅读了完整流程链：`LevelConfig` → `InitConfig` → `InitLevelConfigTileType` → `AssignTileTypeStrategy(V1)` / `AssignTileTypeByDepthStrategy(V2)` → `RefreshLevelConfigByTileAndEffectConfigs` → `InitializeDataList` → `ApplyBatchBindingStrategy` → `InitEntities` → `Board.InitializeFromLevelConfig`
- 深度阅读了 `TileDepthComputer`（空间哈希加速碰撞检测 + Effect 加权 + 序列子牌权重）
- 阅读了 `ByCustomIndexBatchStrategy`（批次绑定）
- 阅读了 `Board.AddTile/AddEffect/UpdateIndexes` 的空间索引逻辑
- 阅读了金牌/火箭替换逻辑（`ModifyTilesToGolden`/`ModifyTileToRocket`）
- 阅读了完整 `LevelConfig` 结构（Tiles/Effects/Piles/TileTypes/BoardSize/DiffStepTimeList/BonusConfigs）

### 输出
`牌局生成全规则.md` — 12 大章节，覆盖：
- 生成流程总览（时序图）
- JSON 结构详解（每字段说明）
- 花色分配（GetColors + ShuffleTileType 步频交换 + V1 统一打乱 / V2 三池分池）
- 配置补全（Size/Life 默认值）
- TileData 构建（SequenceSource → SequenceId 父子关系）
- 批次绑定（ByCustomIndex）
- 棋盘构建（Grid 空间索引 + 可见性 + 金牌/火箭替换）
- 深度计算（TileDepthComputer: SpatialCellSize=8 加速 + Effect 加权 + 序列子牌权重 + 多格 Effect 分组）
- DDA 动态调控（触发条件 + 保护规则）
- 位置生成（JSON 指定 + 序列子牌继承 + 运行时随机）
- 随机种子（LevelMd5 hash 固定种子）
- 完整规则汇总表（20 行）

## 关卡连胜闪电球逻辑梳理

### 输出
生成 `关卡连胜闪电球逻辑梳理.md` — 15 大章节，覆盖：
- 概念总览（连胜计数 vs 闪电球道具）
- 核心数据结构（LevelBaseData 持久化 + LevelCommonData 缓存 + DataBridge 局内传递）
- 功能解锁条件（FunctionUnlock → "TileWinStreak" → 关卡ID阈值，无尽模式默认解锁）
- 连胜计数器生命周期（Init → Start → Win+1 / Fail→0 → Reconnect→0）
- 闪电球道具（LightingProp）：可用性判断、选牌算法（花色最多优先）、消除组数=min(4, 连胜次数)
- 视觉特效（LightingViewAction）：时序参数、资源路径、eff_Lightning01 粒子 + Ani_LightningSphereL{1-4} 动画
- 胜利面板 UI（UIWinStreak → Unlock/Lock 切换 + 进度条动画 + StreakItem 状态机）
- 开始界面策略（StreakUnlock 类型 + WinStreak 清零动画）
- 失败挽留（ActivityExitTempleStreakNode → 连胜+百人七关合并）
- 难度动态调控（InternalControl2 配置表 + 火箭浅层组数影响）
- 数据打点与用户分层（streak_will_3/4, streak_rate）
- ATT 权限弹窗（连胜 > 2 触发）
- GM 调试工具
- 完整数据流图（文本 ASCII art）
- 涉及文件清单（30+ 文件分类）

## RocketVL 闪电球视觉替换实现

### 方案
- 编辑器 Rocket 输入框输入 3 → 火箭牌使用闪电球视觉特效（RocketVL）
- 火箭攻击逻辑不动，仅换视觉层

### 新建文件（2 个）
- `RocketVLLightningViewAction.cs` — 接收 ChainedAttackActionData，展平所有 Wave 目标为 List<TileData>，分 2 组播放闪电球动画 + 闪电击中特效
- `RocketVLLightningViewActionController.cs` — Controller 壳子

### 修改文件（10 个）
- `ICustomViewActionController.cs` — 枚举加 RocketVLLighting
- `CustomData.cs` — 加 RocketMode 字段 + SetRocketMode()
- `DataBridge.cs` — 加 RocketMode 字段（默认 0）
- `TestControlView.cs` — OnRocketEditorEnd 识别 mode=3；OnPlayBtnClick 传 RocketMode
- `LevelEditorData.cs` — 加 RocketMode 字段
- `LevelEditorConfig.cs` — 加 RocketMode 字段
- `LevelEditorConfigWriter.cs` — 写 RocketMode 到 JSON
- `TileMatchViewController.ViewActions.cs` — 注册 RocketVLLightningController
- `TileMatchGame.RecordController.cs` — Trial 模式传 RocketMode 到 DataBridge
- `PrepareChainedAttackActionInvoker.cs` — mode==3 路由到 RocketVLLighting

### 数据流
```
编辑器输入 3 → LevelEditorData.RocketMode=3 → CustomData.SetRocketMode(3)
  → RecordController → DataBridge.RocketMode=3
  → PrepareChainedAttackActionInvoker.ProcessAsync()
    → ResolveControllerType() 检测 mode=3 → RocketVLLighting
    → RocketVLLightningViewAction.DoAction(ChainedAttackActionData)
      → 展平 Waves → 2 组闪电球动画 → AfterAttack
```

### 调试过程
- 开错项目导致首次测试无效
- `DataBridge` 为 `internal` 导致跨程序集不可访问 → 改为 `public`
- `EventType` 歧义（UnityEngine.EventType vs Config.Behaviours.Event.EventType）→ 加 using alias
- 球体动画移除，仅保留闪电打牌

### 输出
生成 `workbuddy_archives/RocketVL-闪电球视觉替换.md`（实现文档）

## 关联

- [[02-PROJECTS/TileMatch/游戏逻辑/Rocket/分析-RocketV2完整逻辑-v2（重构版）|RocketV2 完整逻辑]] — 当日分析基础
- [[02-PROJECTS/TileMatch/游戏逻辑/Rocket/分析-RocketV2技术实现-v1|RocketV2 技术实现]] — 当日生成
- [[02-PROJECTS/TileMatch/游戏逻辑/分析-牌局生成全规则-v1|牌局生成全规则]] — 当日生成
- [[分析-关卡连胜与闪电球逻辑-v1|关卡连胜与闪电球逻辑]] — 当日生成
- [[03-KNOWLEDGE/Game-Logic/游戏逻辑分析|游戏逻辑分析]] — 相关高层文档
