---
title: 2026-09-18 工作记录
date: 2026-09-18
type: daily
status: current
tags: [Daily, LibraryG]
---

# 2026-09-18

## 今日索引

- 完成 INBOX 历史任务包归位：三组 2026-09-03 至 2026-09-04 的流程任务工作区已迁入 `05-ARCHIVE/Agent/`，所有已知 Daily、索引和内部链接已更新。
- 当前 INBOX 只保留四张 2026-09-17 方案卡，以及无来源附件和空 Canvas。

## TileMatch

本轮未读取或修改 TileMatch。

## TileScape

- 做了什么：只读核验 TS 与 MT 共用的底层框架三包（BettaSDK / BettaFramework / BettaInterface），确认其上游仓库、挂载路径、检出 commit 与工作区状态；未修改 TS / MT 任何文件。
- 关键结论：
  - 三包确为**同一批 GitLab submodule 仓库**（SDK / Framework 属 `PhantomFish/BettaFrameWork_Client`，Interface 属 `Sparkwoods/BettaInterface`），"共用"成立；但**挂载路径与检出 commit 均不同**，版本已分叉。
  - 版本实测：BettaSDK MT `1d6f5667`(2026-09-11) 领先 TS `10f609de`(2026-08-07)；BettaFramework 两者不在同一提交线（TS `v0.1.0-81` / MT `v0.1.0-3`）；BettaInterface TS `7096cfd`(2026-08-07) 领先 MT `0b034da`(2026-07-10)。
  - **待处理风险**：TS 侧三个 submodule 工作区均有未提交改动（10 / 4 / 14 个文件，全为净删除），抽样为旧 API 命名回退（`Dispose`→`Release`、`Unregister`→`UnRegister`、删接口重载与注释），即工作区比 HEAD 更旧。本次仅记录，未做任何改动。
- 后续入口：TS 三包工作区与 HEAD 不一致的处置方案需单独评估后再定；跨项目对照代码前先确认具体 commit。

## LibraryG

- 精简 INBOX 收件工具：历史 MCP stdio 与 D1/D2 探针整体归档到 [[05-ARCHIVE/Agent/INBOX收件工具-历史验证-2026-09-18/README|历史验证归档]]；删除空 `fixtures/`、历史依赖锁文件、当前生产不用的依赖声明与可再生成 `node_modules/`。当前工具只保留生产 `src/`、`test/`、README 和最小 `package.json`；`npm test` 只覆盖当前链路。同步将仍提及 `probe:webmcp` 的预演和已替代生产接线方案改为历史说明。验证：无依赖的当前生产测试 9/9 通过，活动工具与工作流文档无遗留可运行旧路径。
- 修复 [[02-PROJECTS/Agent/工具/INBOX收件工具/README|INBOX 收件工具]] 的测试与目录边界：`npm test` 改为只运行当前生产测试；旧 MCP stdio / WebMCP 探针曾独立验证通过，随后按用户要求移至 [[05-ARCHIVE/Agent/INBOX收件工具-历史验证-2026-09-18/README|历史验证归档]]，不再保留运行脚本与依赖。原 MCP 测试取消硬编码日期，改按服务返回的实际文件名并校验日期格式。归档前验证：当前生产测试 9/9、历史测试 3/3 均通过；生产接口仅在本机回环测试中验证，未启动常驻服务或新建真实任务卡。
- 只读诊断 [[02-PROJECTS/Agent/工具/INBOX收件工具/README|INBOX 收件工具]]：生产路径为 `src/webmcp-inbox.mjs` → `webmcp-inbox-server.mjs`（仅监听 `127.0.0.1:38741`）→ 固定 `00-INBOX/`；页面同时提供唯一 WebMCP 建卡工具、粘贴/剪贴板保底入口和暂停开关。生产接口的本机回环测试通过。当前 `npm test` 仍有 1 项失败：`test/inbox-task.test.mjs:116` 将文件名日期硬编码为 `2026-09-17`，实际按本地当前日期创建 `2026-09-18`，属于过期测试断言而非写卡失败。另有历史探针、MCP stdio 与同进程去重模块保留在同一目录，当前生产入口不使用它们，需后续明确拆分或标注后再整理。未修改工具代码、未启动常驻服务、未创建真实任务卡。后续入口：修正测试为注入固定日期或按输入日期断言，并决定是否将历史探针/旧 MCP 模块移入独立历史目录。
- 做了什么：按当前入库与 INBOX 规范复核三组旧任务包的归属，归档 8 份已完成、已验收或已替代的 Agent 工作流任务原件；新增 [[05-ARCHIVE/Agent/README|Agent 历史任务工作区]]，并更新 [[00-INBOX/_MOC|收件箱索引]]、历史 Daily 与所有已知回链。
- 关键结论：旧任务包均非 TileMatch / TileScape 项目事实，也非可迁移通用知识；稳定结论已在 Agent 工作流、项目 MOC 或通用知识 MOC 中。当前四张试用卡继续留在 INBOX；附件与 [[00-INBOX/未命名.canvas|未命名 Canvas]] 仍无来源，保持待确认。
- 产出 / 路径：[[05-ARCHIVE/Agent/README|Agent 历史任务工作区]]；[[00-INBOX/_MOC|00-INBOX 收件箱索引]]。
- 来源依据：[[02-PROJECTS/Agent/工作流/规范-任务产出入库与维护|任务产出入库与维护规范]]、[[02-PROJECTS/Agent/工作流/INBOX对话工作区工作流|INBOX 对话工作区工作流]]、项目与通用知识 MOC，以及用户 2026-09-18 的明确归档授权。
- 验证边界：仅移动 LG 内历史任务工作区并更新 Markdown 链接；未写 TS / MT、未改业务代码、未 commit / push。
- 后续入口：完成任一当前 INBOX 方案卡后，先回链其正式结论或验收，再归档；无来源附件与 Canvas 需用户给出来源后处理。
- 做了什么：新建 `03-KNOWLEDGE/底层框架/` 并入库 4 篇跨项目底层框架文档（1 总览 + 3 分册），挂到 [[03-KNOWLEDGE/_MOC|通用知识 MOC]]。
- 关键结论：TS / MT 共用的 Betta 三包此前无专门梳理，vault 仅 `代码框架总览.md` 有一段项目视角的职责表；本次以包内 README 为底稿转写分册，补上跨项目视角（挂载路径差异、分支策略、版本分叉、TS 工作区异常）。
- 产出 / 路径：[[03-KNOWLEDGE/底层框架/底层框架总览|底层框架总览]]、[[03-KNOWLEDGE/底层框架/BettaSDK/SDK总览|BettaSDK]]、[[03-KNOWLEDGE/底层框架/BettaFramework/框架总览|BettaFramework]]、[[03-KNOWLEDGE/底层框架/BettaInterface/契约总览|BettaInterface]]。
- 来源依据：包内 README + 静态目录/asmdef 扫描 + git 版本核验（2026-09-18）；用户 2026-09-18 确认落点与粒度为「新建目录 + 1 总览 3 分册、完整分册转写 + 补充」。
- 验证边界：文档为静态扫描与 git 实测结论，**未做运行时验证**；未 commit / push。
- 后续入口：[[02-PROJECTS/TileScape/代码框架/代码框架总览|TileScape 代码框架总览]] 后续可回链本组文档，替代"引用以本文档为准"的重复表述。
- 做了什么（第二批）：按"复刻程度"细化 BettaInterface 契约层。逐文件读完 `Packages/BettaInterface` 全部 60 个 .cs（1696 行），删除上轮单篇 `BettaInterface.md`，改建 `BettaInterface/` 子目录 3 篇：[[03-KNOWLEDGE/底层框架/BettaInterface/契约总览|契约总览]] + [[03-KNOWLEDGE/底层框架/BettaInterface/契约清单-上|契约清单·上（基础能力）]] + [[03-KNOWLEDGE/底层框架/BettaInterface/契约清单-下|契约清单·下（业务能力）]]。
- 关键结论：契约层 60 文件覆盖 25 个命名空间、26 个目录；唯一命名空间不一致项是 `Audio` 目录 → `Interface.Sound`。契约基于 **TS 工作区当前版本**（实际编译生效版），与 TS HEAD 有 14 文件未提交差异。已同步更新 _MOC、底层框架总览、BettaSDK/BettaFramework 分册内的回链。
- 来源依据：源码逐文件转写 + 用户 2026-09-18 确认「分档复刻 + 分批递进」。
- 后续入口：下一批细化 BettaFramework 核心机制（UIManager / MessageDispatch / Locale / StateMachine），最后 BettaSDK 架构级。
- 做了什么（第二批·续）：按"复刻程度"细化 BettaFramework 核心机制。深读 `Packages/BettaFramework/Scripts/Runtime` 全部核心源码（UIManager 2414 行 + 窗口体系、MessageDispatch、Locale、StateMachine、Expression、Utils、UICanvas/UIWindow/UIBase），删除上轮单篇 `BettaFramework.md`，改建 `BettaFramework/` 子目录 5 篇：[[03-KNOWLEDGE/底层框架/BettaFramework/框架总览|框架总览]] + [[03-KNOWLEDGE/底层框架/BettaFramework/UIManager-窗口生命周期|UIManager-窗口生命周期]] + [[03-KNOWLEDGE/底层框架/BettaFramework/MessageDispatch-消息总线|MessageDispatch-消息总线]] + [[03-KNOWLEDGE/底层框架/BettaFramework/Locale-本地化|Locale-本地化]] + [[03-KNOWLEDGE/底层框架/BettaFramework/StateMachine-状态机|StateMachine-状态机]]。
- 关键结论：实测解析 `BettaFramework.asmdef` 4 条 GUID 依赖 = `Betta.Interface` + `BettaSDK` + `UnityEngine.UI`(uGUI) + `Unity.TextMeshPro`(TMP)，**修正早前"Framework→SDK 仅 package.json 声明、asmdef 层看不到这条边"的误判**；窗口系统核心是 12 组状态集合 + 串行过渡队列 + 聚焦机制 + 原生屏幕适配桥接（`UtilsNative`/`UtilsDevice` 落在 BettaSDK Native 层，是唯一不可纯抄部分）。已同步更新 _MOC、底层框架总览（依赖图 + 修正说明）、BettaSDK 分册、BettaInterface 契约总览的回链。
- 来源依据：源码逐文件深读 + 用户 2026-09-18 确认「分档复刻 + 分批递进」。
- 后续入口：最后一批 BettaSDK 架构级（ImplementHub / ServiceHub 工厂 / BettaSDKRoot 生命周期 / Config schema / 条件编译宏 / 各 Hub 职责+关键 API）。
- 做了什么（第三批）：按"复刻程度"细化 BettaSDK 架构级。深读 `Packages/BettaSDK` 核心源码（BettaSDKRoot 生命周期 / Singleton+SingletonMono / ImplementHub 191 行 / ServiceHub 工厂+Track partial / ServiceBase+ServiceDefine / BettaSDKConfig+ServiceConfig schema / Hubs / 11 个 Hub 职责与关键 API），删除上轮单篇 `BettaSDK.md`，改建 `BettaSDK/` 子目录 5 篇：[[03-KNOWLEDGE/底层框架/BettaSDK/SDK总览|SDK总览]] + [[03-KNOWLEDGE/底层框架/BettaSDK/启动与生命周期|启动与生命周期]] + [[03-KNOWLEDGE/底层框架/BettaSDK/能力注入-ImplementHub|能力注入-ImplementHub]] + [[03-KNOWLEDGE/底层框架/BettaSDK/第三方服务-ServiceHub|第三方服务-ServiceHub]] + [[03-KNOWLEDGE/底层框架/BettaSDK/配置Schema|配置Schema]]。
- 关键结论：SDK 体量 608 类 / 13.2 万行（另含 2230 内嵌第三方 .cs），只能架构级复刻；`ImplementHub`（191 行，仅依赖 Singleton）与 `Singleton` 可 100% 复刻，`ServiceHub` 是**配置驱动 + 编译期显式工厂（非反射）**；`BettaSDKRoot` 是唯一 MonoBehaviour 驱动源（Awake 初始化 Utils + Update 驱动 ProfileHub/PassportHub/UserHub + 业务层需显式调用 `OnGameReady()`）；自有代码关键条件编译宏 = `PASSPORT_GOOGLE/APPLE/FACEBOOK`、`GameCenter`/`GoogleGameService`、`MODULE_SRDEBUG`、`OBFUZ`、`ENABLE_LOGGING`；asmdef 引用 21 程序集（Betta.Interface + 20 第三方）；`Server`(25k 行)/`Purchase`(6k 行)/`Lattice+Protection`(反外挂) 只写职责不逐类。已同步更新 _MOC、底层框架总览、BettaFramework 各分册、BettaInterface 契约总览、Daily 的回链。
- 来源依据：源码逐文件深读 + 用户 2026-09-18 确认「分档复刻 + 分批递进」。
- 后续入口：三包均已细化到复刻级。
- 做了什么（收口）：老大 2026-09-18 拍板「以 TS 为基准」。收口文档里所有"版本分叉 / 待处理 / 待复核"悬置表述，统一改为「以 TS 工作区实际编译版本为基准，MT 仅作参考；与 HEAD 的旧命名差异不追回」。涉及：[[03-KNOWLEDGE/底层框架/底层框架总览|底层框架总览]]（warning→note、5.3 改"版本对照"、5.4 改"基准决策"）、BettaInterface 契约总览、BettaFramework 框架总览、[[03-KNOWLEDGE/_MOC|通用知识 MOC]] 主题表 + 登记表。
- 关键结论：文档版本口径 = **TS 工作区现状（实际编译生效版）**；不做 `git submodule update` / `checkout` 追 HEAD（避免 API 命名跳变）；若后续升级 submodule 须以本文档为基线比对 API 变更。全程只读 TS / MT，未 commit / push。
