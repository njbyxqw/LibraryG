---
title: 2026-09-17 工作记录
date: 2026-09-17
type: daily
status: current
tags: [Daily, LibraryG]
---

# 2026-09-17

## TileMatch

本轮未读取或修改 TileMatch。

## TileScape

本轮未读取或修改 TileScape。

## LibraryG

- INBOX 归位巡检：依据入库规范、INBOX 工作流及项目/通用知识 MOC 核对全部现有材料。结论是 2026-09-03/04 的旧任务包、执行反馈和验收均为 Agent 工作流历史证据，稳定结论已在 `02-PROJECTS/Agent/工作流/`；不应迁入 TileMatch、TileScape 或 `03-KNOWLEDGE`，仅列为 `05-ARCHIVE/` 候选，待用户确认物理移动。3 张 2026-09-17 卡仍为当前方案任务，继续留 INBOX；附件与空 Canvas 无来源，维持待确认。更新 [[00-INBOX/_MOC|收件箱索引]]，并将旧 `inbox-organizer` 表述修正为历史说明。来源：当前 LG 规范与已回链正式文档；未移动或删除文件。
- 整理 `00-INBOX/`：新增 [[00-INBOX/_MOC|00-INBOX 收件箱索引]]，将 3 张 2026-09-17 WebMCP/粘贴交接方案卡列为当前待处理；将 2026-09-03 MOC 路由工作区、2026-09-04 已验收审查包及已替代桥接包明确列为历史留档。未移动或删除任何原始材料。`attachments/` 下 7 张图片与 [[00-INBOX/未命名.canvas|未命名 Canvas]] 均无明确来源，保留待确认；不从文件名或时间推断归属。同步从 [[02-PROJECTS/Agent/工作流/INBOX对话工作区工作流|INBOX 工作流]] 回链该索引。后续入口：若要物理归档历史文件夹或处理未归属材料，需先确认具体范围与来源。
- 用户确认：暂停 Chat-on-St​eroids 采用路线，优先设计自建的最小权限 INBOX 收件工具。
- 新增 [[02-PROJECTS/Agent/工作流/方案-自建INBOX收件工具|自建 INBOX 收件工具设计]] 并挂入 [[02-PROJECTS/Agent/工作流/_MOC|Agent 工作流 MOC]]。设计限定唯一 `create_inbox_task` 工具、固定 `00-INBOX/`、排他新建、无读取/终端/桌面/项目访问，按 D0–D4 分阶段授权；当前仅完成 D0 草案，未创建服务、代码、密钥、tunnel 或 ChatGPT App。
- 用户确认后进入 D1：在 [[02-PROJECTS/Agent/工具/INBOX收件工具/README|INBOX 收件工具目录]] 创建 Node.js 本地核心与临时目录测试。`npm test` 前四项通过：合法新建、同标题不覆盖、路径/枚举拒绝、正文不改变 frontmatter；未写真实 `00-INBOX/`，未启动网络服务或连接 ChatGPT。
- 完成 D2a（本机 MCP 适配）：采用官方 MCP SDK 的 stdio 方式，仅注册 `create_inbox_task`；协议 client 集成测试确认工具发现只返回这一项，写入仍仅在系统临时目录。当前 `npm test` 5/5 通过。未监听网络、未建 tunnel、未读取 ChatGPT 凭据或连接 App。D2b 涉及账号设置、私有连接与可能的平台写入确认，待实施前单独授权。
- D2b 官方能力核验：当前完整 MCP（含写入/修改）只在 ChatGPT Business、Enterprise / Edu 的 beta 中提供；个人 Plus / Pro 不在完整写入 MCP 的可用范围内，其中 Pro 被官方明确限制为 read/fetch。ChatGPT 也不能直连本机 stdio 服务，符合资格时需 Secure MCP Tunnel 或等价官方私有远程连接。当前无法读取本机浏览器会话权限策略，尚未核实用户工作区资格；因此没有启动 tunnel、远程服务、App 创建或真实写卡。若为个人 Plus / Pro，使用“Chat 任务包 → 用户明确交给 Codex → 本地 writer”后备流程，不绕过权限。
- 用户要求重做方案：不再把 Chat 直接写本机 INBOX 当作个人 Plus 的目标。新增 [[02-PROJECTS/Agent/工作流/方案-ChatGPTPlus环境INBOX收件与交接|Plus 环境 INBOX 收件与交接方案]]：P0 先核验前提（已完成），P1 才试用“Chat 固定任务包 → 用户交给 Codex → 本地 writer”，累计 3–5 次后再决定是否设计显式触发的本机快捷方式。当前不继续 D2b、不采用 CoS / 浏览器桥接，也不启动 tunnel。
- GitHub 方向筛选：`save-to-obsidian`、`chatbridge`、`chatgpt-ux-navigator` 证明“Chat 页面扩展 → `127.0.0.1` helper → 本地 Obsidian 写入”不依赖 ChatGPT 写入 MCP，可在 Plus 下作为未来 P3 的技术方向；但现有项目分别偏完整会话保存、Obsidian REST API/低成熟度、或包含远超需求的 prompt 注入/编排能力，均不直接安装。`Codexless` 和 FileBridge 仍依赖自定义 MCP/tunnel，不能解决 Plus 写入资格。方案更新为：先 P1 任务包试用；若有证据证明必要，再自建带可见确认按钮、仅写 `00-INBOX/` 的页面收件器。
- 用户确认方向为“Chat 形成方案后自动导出到本地”，据此新增 [[02-PROJECTS/Agent/工作流/方案-ChatGPTPlus自动INBOX收件器|ChatGPT Plus 自动 INBOX 收件器设计]]。设计不再使用 MCP：ChatGPT 网页扩展只读取当前最后一轮的固定 JSON 任务包，经 `127.0.0.1` helper 和随机 token 复用现有 writer，只能新建 `00-INBOX/` 任务卡。D0 仅完成需求/边界设计；D1–D5 均未开始，尤其未安装扩展、未监听端口、未写真实任务卡。待确认浏览器、触发词集合及“先按钮后自动”的上线顺序。
- 桌面端补充调研：官方桌面应用内置浏览器提供 Site Tools / WebMCP，理论上可由一个本机 INBOX 页面暴露唯一建卡动作，不依赖 custom MCP；但官方仅说明“账号、模型与页面支持时可用”，未确认个人 Plus、普通 Chat 会话或 `localhost` 页面可行。此路线仅记为待验证候选；没有创建本机页面、开启 Site Tools 或改变权限。
- 机制澄清：CoS 的“桌面”是其自身 Electron 本地控制台，Chat 实际仍在 Chrome/Edge 的 ChatGPT 网页；其路径为 custom MCP + tunnel → CoS Core，Chrome 伴侣扩展另负责观察/自动化 ChatGPT DOM 和本地会话记录。它不适用于 Plus 的写入 MCP，也不应与 ChatGPT 桌面端 Site Tools 混同。
- 新增 [[02-PROJECTS/Agent/工作流/预演-ChatGPT桌面端WebMCP自动INBOX收件|桌面端 WebMCP 自动 INBOX 收件理论预演]]：将桌面路线收敛为 H1–H7 硬门槛。关键修正是首测仍只暴露最终的 `create_inbox_task`，但 helper 固定写入临时测试目录；不增加 `status` 等额外能力、不让模型传根目录。平台确认不是由 `consequentialHint` 保证，若实测没有清晰确认，不能声称“一句话静默建卡”，应停在用户显式二次确认或回退网页扩展。当前仍未启动页面、服务或真实 INBOX 写入。
- 用户授权制作最简 D1 探针：在 `02-PROJECTS/Agent/工具/INBOX收件工具/` 新增 `src/webmcp-probe.mjs` / `webmcp-probe-server.mjs`。启动后仅绑定 `127.0.0.1:38741`，每次使用系统临时目录作为固定测试根目录；页面仅用 `document.modelContext.registerTool` 声明 `create_inbox_task`，无读取、列表、删除、路径或根目录参数。`npm test` 6/6 通过（含页面工具边界与原有 writer/MCP 测试）。未启动探针、未打开 ChatGPT 桌面内置浏览器、未创建任何真实或临时测试卡，H1–H6 仍待用户参与实测。
- D1 首次运行实测：用户在当前桌面应用内置浏览器打开 `http://127.0.0.1:38741/`，页面显示 `document.modelContext` 与唯一工具注册成功；浏览器工具层发现 `create_inbox_task`。按用户给定字段调用后成功返回 `taskId` `8bbe332f-147e-4dcb-a58a-1a083fa2d3f9` 与测试卡 `2026-09-17 - WebMCP 首次测试.md`。该卡仅在本次进程的系统临时根目录，未写真实 `00-INBOX/`。已验证页面 API、工具发现和隔离写卡；此次由 Codex 浏览器工具适配层直接调用，未证明普通 Chat 自然语言自动选工具，且未观察到用户确认弹窗，相关授权/日常可用性仍待验证。
- 为补 H4，D1 页面改为在每次 `create_inbox_task` 前显示原生确认框，明确展示标题、状态、目标及“只写临时目录”；用户取消则零写入，不依赖 `consequentialHint` 是否被平台实现。`npm test` 仍为 6/6。运行中的旧探针尚未重启，确认框尚未做端到端实测；不停止用户终端中的进程，待用户手动重启后继续。
- H4 端到端实测修正：重启后页面加载“每次建卡将由本机页面再次确认”；用户确认看到了原生弹窗，并对 `WebMCP 确认层测试` 选择确认，成功创建临时卡 `2026-09-17 - WebMCP 确认层测试.md`（`taskId` `c15e063a-3bcb-4e73-94b9-c2c95472f8fd`）。随后用户对 `WebMCP 取消确认测试` 选择取消，调用在 `fetch` 前以“用户取消创建测试卡”拒绝，未进入 helper 写卡接口。自动化无法读取该弹窗，且取消错误使浏览器工具桥重置，未独立枚举临时目录；但“用户可见确认”及“取消不触发写卡请求”均有实证。生产方案可保留页面确认闸门，但不能把自动化侧是否收到对话框作为判断依据。
- 自然语言整理到任务包测试：根据本对话形成的桌面 WebMCP 验证结论，助手归纳字段后调用同一工具，用户在页面确认后成功创建临时方案卡 `2026-09-17 - 桌面端 WebMCP INBOX 收件验证.md`（`taskId` `c1f8d78a-bab1-4d00-98de-92d6192ddf6e`）。已实证“聊天结论 → 受限结构化任务包 → 用户页面确认 → 隔离建卡”；不等同于任意普通 Chat 会话都会自行选工具，生产接线和日常试用仍未开始。
- D2 最小实现：新增内部任务摘要去重，探针同一进程内的相同输入再次调用时只返回首次结果（`reused: true`），不创建第二张卡；页面增加创建中、已创建、已取消和失败提示。新增直接去重测试，`npm test` 7/7 通过，仍全在系统临时目录。该去重尚不跨进程持久化，且运行中的旧探针未加载 D2；重启后才进行端到端重放测试，真实 `00-INBOX/` 未接入。
- D2 端到端重放验证通过：用户重启后对同一 `WebMCP D2 重复提交验证` 连续两次确认。第一次返回 `taskId` `0a7b6e28-14ac-44a4-abbf-2c178e5d83e4`、`reused: false`；第二次返回相同 ID/路径和 `reused: true`，未新建第二张临时卡。仅证明同进程去重；跨进程去重、真实 `00-INBOX/` 接线均未开始。
- 进入 D3 生产前设计，新增 [[02-PROJECTS/Agent/工作流/方案-桌面端WebMCP生产INBOX接线|桌面端 WebMCP 生产 INBOX 接线设计]]：生产仍只有 `create_inbox_task` 和页面确认；固定根目录为 `00-INBOX/`，另用被 Git 忽略的 `.inbox-receiver/receipts.json` 保存不含正文的短期收据，默认 30 分钟跨重启去重。先做设计和验收矩阵，未创建收据、未改 `.gitignore`、未修改生产入口、未写真实 INBOX。
- D3 生产组件实现完成但未启动：新增私有持久收据模块、独立 `serve:webmcp-inbox` 入口（仅 `127.0.0.1:38742`）及生产页面。收据采用“先 pending、建卡、完成收据”的保守顺序；中断 pending 会拒绝重试而非重复建卡。根 `.gitignore` 已忽略 `.inbox-receiver/`；`npm test` 9/9 通过，覆盖跨 helper 实例 30 分钟重放和过期后新建，所有测试根目录均为系统临时目录。未启动生产入口、未创建真实 `00-INBOX/` 卡或实际收据。
- 用户确认真实试用后，已启动 `serve:webmcp-inbox`（`127.0.0.1:38742`），修正生产页残留“临时测试目录”文案并新增回归测试，`npm test` 10/10 通过。用户在本机确认框确认后，真实 `00-INBOX/` 新建 [[00-INBOX/2026-09-17 - WebMCP 生产 INBOX 首次试用|WebMCP 生产 INBOX 首次试用]]，`taskId` `4ad9e27a-e913-4341-87e4-58a67c566205`、`reused: false`；静态核对格式/状态/下一步/关联均正确。`.inbox-receiver/` 已作为 Git 忽略的私有收据状态产生；生产入口仍在运行。尚未验证真实目录跨重启重放，未进入日常自动试用。
- 用户调整生产策略为“自动写入后通知 + 暂停/恢复总开关”：移除生产端每卡确认、跨重启 30 分钟去重及频率上限；保留固定 `00-INBOX/`、唯一工具、原子新建和不覆盖。新增 Git 忽略的 `settings.json` 保存自动建卡状态，默认开启、暂停跨重启保留；页面显示写后通知。删除未使用的持久收据模块和测试，`npm test` 9/9 通过，均为临时目录/静态测试。新增 [[02-PROJECTS/Agent/工作流/方案-桌面端WebMCP自动通知模式|自动通知模式]]；当前生产进程仍为旧版，尚未重启加载新策略。
- 自动通知模式已重启加载并实测：生产页显示“自动建卡：已开启”及暂停按钮；无需确认框即成功新建 [[00-INBOX/2026-09-17 - WebMCP 自动通知模式试用|WebMCP 自动通知模式试用]]（`taskId` `23c50b54-9484-49bf-b1d3-fa51c0bd79d9`）。暂停后工具调用以“自动建卡已暂停”拒绝，未创建卡；随后已恢复开启。已删除废弃的 `.inbox-receiver/receipts.json`，保留 Git 忽略的 `settings.json` 仅记录自动开关；生产收件器仍在 `127.0.0.1:38742` 运行。
- 验收发现普通 Chat 会在未调用网站工具时生成临时预览文件，且 `38741` 的历史探针与 `38742` 的生产入口造成理解歧义。按用户要求调整为唯一生产入口 `http://127.0.0.1:38741/`，停止旧探针与旧生产进程；历史探针改到 `38743`。页面新增“粘贴任务交接包”保底通道，裸 JSON 或 `json` 代码块经同一受限接口写入固定 `00-INBOX/`。`npm test` 10/10 通过（新增回环接口测试，测试写入均在系统临时目录）。真实页面创建 [[00-INBOX/2026-09-17 - INBOX 粘贴交接通道试用|粘贴交接通道试用]] 成功；暂停后同一粘贴路径被拒绝，随后已恢复开启。当前未证明普通 Chat 自然语言会稳定调用 WebMCP，故将其定位为快捷通道，粘贴交接包为可验收保底路径。
- 继续降低保底操作数：页面新增“从剪贴板创建”。用户在 Chat 点复制 JSON 交接包后，在收件页点一次即可创建；页面只在该用户点击时调用剪贴板读取，不做后台监听或剪贴板留存，浏览器拒绝时仍可手动粘贴。生产进程已重启加载该版本，页面实测显示入口且 WebMCP 工具仍注册；`npm test` 10/10 通过。尚待普通 Chat 的真实复制内容完成一次用户体验验收。
- 需求回归：确认普通 Chat 一句话自动落地仍未通过，复制导入只能算后备。修复代码块解析与并发请求处理、拒绝额外字段、移除测试日期写死；新增固定 Chat 说明及完整路径/执行端读取指令。12 项测试通过；刷新用户保留的旧探针页后，通过剪贴板按钮真实新建 [[00-INBOX/2026-09-17 - INBOX 完整交接验收]]，本机 Codex 读取核对内容。来源为本轮代码检查和 UI 实测；普通 Chat 发起端及远程 Work 未验证，后续入口为 [[02-PROJECTS/Agent/工作流/方案-桌面端WebMCP自动通知模式#需求回归与验收（2026-09-17）]]。
