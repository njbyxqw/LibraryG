---
title: 2026-09-14 工作记录
date: 2026-09-14
type: daily
status: current
tags: [Daily, TileScape, LibraryG]
---

# 2026-09-14

## 今日索引

- 本次涉及项目：TileScape、LibraryG。
- 完成事项：继续 DDAV3 R07–R08，追踪 `ReservedSpace` 交叠，并把 M01/M07 从抽象指标行改成真实 DDA 触发状态草图。
- 关键结论：Mystery reservation 能与另一逻辑事件的 DDA 结构性交叠；而实际样本还必须由点击/序列揭牌产生非空 DDA 目标集合，单有可见 A 牌面并不足够。当前只写出静态触发草图，未运行验证。
- 产出文档：[[02-PROJECTS/TileScape/游戏逻辑/任务-DDAV3逻辑策略持续研究|DDAV3 持续研究主档]]。
- 来源版本：R07–R08 结论来自 `DDA-V3-Test @ cd3aea16656814e0e226205c3c225c36a94deded`；当前 checkout 位于 `feat-winStreak @ 3d1d339c7ac4010002ea02af190ed4f46c9c52a4`。工作区已有未提交文件，本轮未触碰。
- 验证边界：源码/配置/测试源码静态追踪；未运行 Unity、自动化牌局或测试；未构造合法运行样本。
- 后续入口：研究主档 R08 下一断点，核实 `Levels/new/2.json` 顶层/揭牌关系与最小 V2 DDA fixture 装配项。

## TileMatch

本轮未读取或修改 TileMatch，未形成该项目结论。

## TileScape

### DDAV3 持续任务 R07：ReservedSpace 与 DDA 快照

- 追踪 `Mystery.json` 的 Capture → 动画/销毁 → Commit → ActionResult 完成回调链。Commit 才对目标牌加锁并预留 1 格；`CompleteTransaction` 在转发 `TileClicked` 前释放预留并解锁。
- 普通 `AddToBarAction` 先移除 Board/OverBar，再 `Bar.AddTile`，之后才 `Board.InvokeLevelDDA`；`Bar.AddTile` 会按该牌 ID 释放 Reservation 并更新 BarStatus。因此同一 Mystery 转发点击的正常 DDA 时点已不包含其自身 reservation。
- 当前 `ReserveBarSpaceAction` / `ReleaseBarSpaceAction` 未发现 JSON 配置引用；本仓库静态配置中只有 Mystery 的 HandleForwardTileClick 使用 reservation。不能由此排除运行时配置或异步在途的其他 reservation。
- 现有测试分别覆盖 Mystery 配置契约、HandleForward 完成幂等/ID 契约、表现层点击可见性；未覆盖 reservation 生命周期和 DDA 快照的时序。本轮只读测试源码，未执行。
- 续查确认不同 `ActionExecutionUnit` 可并发 await；Mystery 的 ViewAction 等待约 0.63 秒后才通过 `finally` 完成转发并释放 reservation。`UIGamePanel` 交互门禁按 GameState，`TileView` 则核验单牌锁/高亮/状态，未见全局 `Application.Pending` 点击门禁。因此已有异步结构下，另一张合格牌可在 reservation 存续期进入 `AddToBarAction` 并调用 `Board.InvokeLevelDDA`。这是代码路径可达性结论，不是玩家运行复现。
- 测试盘点：`MoveToOverBarPropTests.CreateContext` 合成了 GameContext、逻辑服务、Board/Bar/OverBar/SequenceControl，可作为 M01/M07 上下文夹具起点；`MeatloafDeltaSequenceConstraintContractTests` 另有反射构造 BoardData/SequenceData 的轻量方式。未找到完整 TileMatchGameLogic 集成夹具。本轮无代码改动、无测试运行。
- 后续入口：将 M01、M07a/b 映射到现有合成夹具，列出需补的遮挡/可见性/Bar计数和观测点，同时形成 Mystery→第二点击→DDA 的可复现步骤；再判断运行验证层级。

### DDAV3 持续任务 R08：指标行到真实 DDA 触发态

- 核对 `AddToBarAction` 的真实调控入参：点击牌先从 Board 移除并收集揭牌 `regulationList`，再入 Bar，最后把该列表交给 DDA；列表为空时直接不执行。因此 R04 的 M01 指标行若要成为实际调控样本，需让点击遮挡牌 X 揭出 A，并确认 A 确实进入本次调控目标。
- 写出一个后点击快照为 `F=3` 的 M01 草图：Bar 有 A×2 和另一 singleton B；点击覆盖目标 A 的 X 后 X 入 Bar，7 格栏 UnmatchedCount 为 4；覆层移除后 A 作为揭牌目标。仍需验证 A 的实际可见/HasSeen 状态及 `regulationList`。
- M07a/b 共享该揭牌触发几何，差别是完成 A×3 的牌处于 OverBar 顶部 `y=0`（可点）或被压在 `y=1`（不可立即点），只验证资格与模型漏计边界。
- 找到受跟踪候选 `Assets/Game/TileV2/Editor/LevelConfig/Levels/new/2.json`：18 个牌位，无 Effects/Piles，在 `(6,7)` 具有 `z=0/z=2` 共格；可作为几何来源候选但 LevelId=0、注释“不需连续引导”，类型池随机分配，未验证可运行性。未读取工作区未提交 Temp 牌面。
- 合成测试上下文可复用 Board/Bar/OverBar 构造，但当前 builder 的牌位不重叠、关闭了可见性更新，也未设置 V2 DDA；需要补充几何、目标集合、DDA 配置与服务后才可测完整调控选择。
- 本轮纯静态追踪及跟踪文件数据盘点，未运行测试或 Unity。
- 后续：检查候选 LevelConfig 标准化后的揭牌关系/类型池，并盘点最小 V2 DDA fixture 配置项；再把输入序列与日志观测字段落成运行步骤。

### DDAV3 持续任务 R09：研究分支与当前 checkout 分流

- 新检查发现当前分支为 `feat-winStreak @ 3d1d339c7`，与 `DDA-V3-Test @ cd3aea166` 从共同提交 `5ac5062` 分叉；V3 分支引用仍可读。
- 当前 feat 分支删掉 `InLevelDDAV2SlackController.cs`，V2 策略恢复为先抽取 `SelectStrategy()` 概率档位，再执行策略；HardSlack/FastNCD 控制器和相关诊断不存在。Slack 专属 helper 契约测试也被删除。
- R01–R08 因此只对 DDA-V3-Test@cd3 有效，不代表当前 checkout。后续继续基于 V3 ref 只读研究；未切分支、未建 worktree、未触碰已有未提交状态。Unity 验证需有包含 V3 的可运行环境，本轮未运行任何测试。
- 产出与后续：主档 R09 标注分流；下一步从 DDA-V3-Test ref 完成样本几何和最小测试装配清单，避免混用当前 feat 代码。

### DDAV3 持续任务 R10：揭牌资格与随机牌池

- 在 `DDA-V3-Test@cd3aea166` 静态确认 `Levels/new/2.json` 有18张默认 Random 牌、3类均衡后整体洗牌（每类6张，坐标牌型不固定）；`(6,7,z=2)` 覆盖 `(6,7,z=0)`，揭出底牌只有从未见过转为 Highlight/Visible、允许加入 DDA 且 TileConfig 能力许可时才进入调控列表。
- 该关卡适合自然揭牌几何观察，不适合作为可控 M01/M07 固定牌型反例；应由合成 LevelConfig 固定牌型和重叠关系。既有测试上下文需补 LevelConfig、TileConfigs、RandomService、V2 InLevelDDA、重叠 BoardData 和开启可见性刷新的移除路径。
- 来源：`LevelConfig.cs`、`LevelConfigLoader.cs`、`AssignTileTypeByDepthStrategy.cs`、`TileData.cs`、`Tile.cs`、`Board.cs` 与 `Levels/new/2.json`，均从 DDA 分支引用读取。未切换分支、未运行 Unity/测试；结论是静态源码闭环。
- 后续入口：研究主档 R10 下一断点，逐项核对最小上下文夹具依赖与 V2 DDA 挂载方式，再区分 EditMode 可验证范围和必须在 V3 Unity 环境确认的策略/表现结果。

### DDAV3 持续任务 R11：V2 调控夹具装配层级

- 盘点 `MoveToOverBarPropTests.CreateContext`：GameContext、GameData.SequenceData、基础服务、Statistic、Board/Bar/OverBar、SequenceControl 与全局 GameContext 均可复用；它用 `board.AddTile(tile,false)`，未验证重叠揭牌。
- `InLevelDDA(context,V2,v1Config,v2Config)` 会构造 V2 策略；Slack 核心依赖 Board/Bar/OverBar、TileConfigs、LevelConfig、RandomService、Statistic。实际交换还依赖 TileService 与 EventDispatchService。因此分为目标收集层和 V3 策略交换层；当前 checkout 不含控制器，未运行测试。
- 下一步：主档 R11 将 M01/M07 改成分层测试规格，分别列出揭牌目标断言和 V3 快照/策略/交换观察字段；继续从分支引用只读分析，不编辑 TS 测试。

### DDAV3 持续任务 R12：M01/M07 分层验证规格

- Layer A 可在 EditMode 上下文层核对固定 V2 配置下的重叠揭牌：A 初始 NotVisible/HasSeen=false，移除 X 并刷新可见性后转 Highlight/Visible、置揭牌触发标记并进入 regulationList；另用 DDA 资格关闭作排除对照。
- Layer B 必须在含 V3 控制器的 Unity 环境：按 M01 固定 Bar A×2+B、X 覆盖 A，核对点击入槽后 `UnmatchedCount=4`、`F=3` 和 Slack 决策；M07a/b 共用触发几何，只切换第三张 A 在 OverBar 的 y=0/y=1。检查决策后交换坐标与后续可点资格。
- V3 开发日志字段包括 source、曲线/进度/目标与当前HardSlack、候选范围、选择指标、策略、fallback、noSwapReason，但不含候选实体ID。静态无法判定具体候选、指标结果、是否换牌及动画时序。当前分支无 Slack 控制器，本轮未执行用例。
- 后续入口：主档 R12 下一断点，复核 M01/M07 条件是否可同时满足并调整最小状态；只读分支源码，不改 TS 代码。

### DDAV3 持续任务 R13：M01/M07 共存与初始 HasSeen

- 静态确认 M01 和 M07a/b 可放进同一合成快照：Board 的 X 是独立普通牌型、覆盖新揭 A；Bar 初始 A×2+B×1；OverBar 预置第三张 A，只切换 y=0/y=1。点击 X 后 `UnmatchedCount=4`、7格栏且无其他预留时 `F=3`。这证明状态无结构冲突，不证明此前 OverBar 牌面在真实局中如何形成。
- 发现测试初始化风险：TileData 构造默认索引会先按 Highlight 路径处理可见性，可能污染 `HasSeen`。正式 `LevelEnterAnimationStepOneFinished` 会调用 `InitializeSeenStateFromCurrentVisibility` 按最终可见性重建状态。夹具应完整加牌、统一更新索引和可见性，再重建 HasSeen，之后测试揭牌。
- 当前分支/HEAD 未变：`feat-winStreak@3d1d339c7`，研究 ref `DDA-V3-Test@cd3aea166`；工作区出现更多未提交 Tutorial/UI/配置改动，本轮未读取或触碰。全为源码静态核对，未运行测试。
- 后续入口：主档 R13 下一断点，核对正式初始化的 DDA/随机类型/TileData/Board/HasSeen 顺序，确认 Layer A 是否能复用正式时序。

### DDAV3 持续任务 R14：正式建局顺序

- V3 分支构造顺序核对：V2 先走 LevelTileTypeInit 的随机牌型分配，再建 TileData 和 Board；InitEntities 完成索引/显隐后才调用 InitInLevelDDA。`LevelEnterAnimationStepOneFinished` 稍后按最终显隐重建 HasSeen。构造期间 RandomService 在 Config、Entities 和 DDA 初始化后分别重置。
- Layer A 应按 TileData/Board 初始刷新 → 挂 V2 DDA → 重建 HasSeen → 移除覆盖 X 的顺序；固定牌面时可直接设置最终类型，但需标明省略正式随机池初始化。全为 `DDA-V3-Test@cd3aea166` 源码静态核对，未执行测试。
- 后续入口：主档 R14，比对正式初始化与 `CreateContext` 现有构造器，收敛夹具需要新增的调用并确认 internal API 边界。

### DDAV3 持续任务 R15：Layer A 夹具差异收敛

- `MoveToOverBarPropTests.CreateContext` 可复用 Context、服务、Statistic、Board/Bar/OverBar/SequenceControl；已有测试直接使用 Context internal setter。其牌位都在不同坐标且 `AddTile(false)`，没有覆盖与初始可见性重建。
- TileConfig 的 `CanJoinDDA` 为 private set 且默认 true；资格排除对照可调用 internal `TileData.UpdateCanJoinDDA(false)`。Layer A 可移除 builder 预置牌、换成两张重叠固定坐标牌，再全量更新索引/可见性，挂 V2 DDA、重建 Seen，随后真实 RemoveTile 收集目标；此数据层断言不覆盖完整 ECA/表现链或 Slack 策略选择。
- 本轮仅整理静态可执行步骤，未运行夹具。后续按主档 R15 检查 G1–G6 完成标准和剩余静态缺口，必要时继续推进；研究完成后保留提醒自动化。

## LibraryG

- 按持续任务主档更新本批证据、范围和下一断点；现有 TileScape MOC 已链接主档，无需变更索引。
