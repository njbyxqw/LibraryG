---
date: 2026-09-03
weekday: Wednesday
tags: [daily, LibraryG, Git]
source: "用户确认：GitHub 仓库基于旧 LibraryG 建立；本机后续变动未提交。静态 Git 基线核对。"
---

# 2026-09-03 工作日志

## 今日索引

- 涉及项目：LibraryG / LG。
- 完成事项：将本机 LG 与 GitHub `njbyxqw/LibraryG` 的 `main` 建立跟踪关系，并以远端旧快照完成初步差异盘点。
- 关键结论：本机工作区未被覆盖；当前 `main` 已跟踪 `origin/main`，本机相对远端存在修改、删除与新增，反映旧目录结构后的持续演进，暂不适合直接整体提交。
- 产出文档：[[01-DAILY/2026-09-03|2026-09-03 Daily]]。
- 后续入口：先审查并恢复/更新 `.gitignore`，再按“配置与缓存、历史归档、稳定知识文档”分批确认暂存范围。

## TileMatch

无。

## TileScape

无。

## LibraryG

完成 Git 关联与初步评估：本机 `main` 已跟踪 GitHub 远端 `origin/main`；远端基线为 `ae41b0e`（2026-09-03，`init: LibraryG Obsidian vault 初始提交`）。本机文件未被检出或覆盖。

### Git 基线与初步盘点

- 做了什么：初始化本机 Git 元数据，绑定 `origin`，获取远端 `main`，并将本机分支安全定位到远端基线。
- 关键结论：当前 Git 状态为约 110 个已修改、113 个远端已跟踪但本机不存在的文件，以及 46 条未跟踪项；删除项主要集中在旧 `00-INBOX/`、`Excalidraw/` 和旧 `02-PROJECTS/WorkBuddy/` 路径，符合后续目录迁移/清理的可能，但尚未逐项判定是否应提交删除。
- 风险与待确认：本机缺少远端的 `.gitignore`，因此 `.smart-env/`、工作区状态、个人记忆与数据库等机器生成内容会显示为未跟踪；在恢复或重建忽略规则前，不应执行整体暂存。
- 产出 / 路径：Git 远端 `https://github.com/njbyxqw/LibraryG.git`；本日志。
- 来源依据：用户确认的仓库来源；Git 静态快照 `origin/main` 与本机工作区对比。未根据文件修改时间推断个人工作。
- 后续入口：[[02-PROJECTS/Agent/工作流/规范-任务产出入库与维护|任务产出入库与维护规范]]；下一步单独审查 `.gitignore` 与提交分组。

### 缺失文件与 `.gitignore` 审查

- 缺失文件分类：70 个旧 `Excalidraw/` 素材或绘图文件、17 个旧 `00-INBOX/attachments/` 附件、9 个旧 INBOX 工具/预览文件、11 个旧 `02-PROJECTS/WorkBuddy/` 文件、2 个项目文档、2 个 Daily，以及根目录 `.gitignore` 和 `README.md`。
- 迁移边界：Git 未识别出可自动确认的重命名；旧 WorkBuddy 路径与当前 `02-PROJECTS/Agent/` 的关系应按内容而非文件名逐项确认。
- `.gitignore` 审查：远端规则已覆盖 `.DS_Store`、`.smart-env/`、`.workbuddy/memory/`、Obsidian 工作区/缓存、SQLite 数据库与回收站；本机上述主要生成项均存在。建议先恢复这份规则作为基线，暂不将 `.obsidian/plugins/` 整体忽略，因为远端本来就版本管理插件文件。

### 旧 LG 补充与双设备配置结论

- 路径迁移已确认：旧 `02-PROJECTS/WorkBuddy/` 的 11 个文件在当前 `02-PROJECTS/Agent/` 均有对应文件，其中 7 个内容完全一致、4 个为后续更新版本；应保留当前 Agent 路径，不再恢复旧路径副本。
- 应恢复/归位：两篇缺失 Daily；TileMatch `分析-关卡间隔时长SQL-v3.md`（`finalized`，2026-08-28，当前无同主题替代）；TileScape `手套道具MoveToOverBar表现层分析.md`（当前概览未覆盖旧文档的提交与表现层细节）；以及仍被当前 Canvas / 插件说明引用的两项 Excalidraw 资产。
- 应归档而非放回活动区：其余旧 `00-INBOX/` 附件和工具、未被当前文本引用的 Excalidraw 素材；保留其 Git 历史，并在后续整理时放入 `05-ARCHIVE/` 的旧 LG 快照区。
- 可确认删除：旧 `README.md` 仅含 `# LibraryG`；`00-INBOX/未命名.md` 为空。其余删除项尚不以“无用”判定。
- 双设备配置原则：共享 vault 语义配置、忽略机器状态和缓存；现有 `.obsidian/plugins/obsidian-local-rest-api/data.json` 含 `apiKey` / `crypto` 字段，Excalidraw 插件数据含多个 AI/API 字段，须作为设备私有配置处理，不能无差别同步到 Git。

### 已实施的补充与兼容整理

- 已恢复到活动路径：2026-07-31、2026-08-10 Daily；TileMatch 关卡间隔时长 SQL；TileScape 手套 MoveToOverBar 表现层历史审计。
- 已归档：远端旧 `00-INBOX/` 快照至 [[05-ARCHIVE/Legacy-LG-remote-2026-09-03/README|旧 LG 远端快照归档]]，不再混入当前活动 INBOX。旧 `Excalidraw/` 按本机临时工作区策略移出共享快照，仍可从 `origin/main` 基线追溯。
- 已建立跨设备规则：新增 `.gitignore`（机器状态、缓存、Excalidraw 临时区与 SQLite 运行时侧车文件）及 `.gitattributes`（共享文本统一 LF）；插件安装包和私有插件数据的迁移已从本次候选中拆出，当前仍按 Windows 基线追踪；README 重建为跨设备入口说明。
- 用户确认：`.workbuddy/memory/` 中的用户级/项目级记忆与 `level_tracker.db` 均为本人维护的共享资料，已改为纳入 Git；仅忽略 SQLite 的 `-journal`、`-wal`、`-shm` 运行时侧车文件。
- 暂存边界：已将当前主版本、旧快照归档、跨设备配置、记忆与数据库统一暂存，未提交/推送；静态文本检查仅发现既有历史 Markdown 与 WorkBuddy 模板的尾随空白，未为格式检查批量改写历史内容。

### 双设备共享 LG 设计复核（更正）

- 用户确认：`origin/main` 即 Windows 的既有 LG。当前 Mac 工作区相对该远端的暂存整合，正是将 Mac 后续内容与 Windows 基线合为共享 LG 的候选快照；无需另建 Windows 导入分支。
- 当前状态：Mac 的内容、恢复的历史资料、归档目录、用户级/项目级 Markdown 记忆与 SQLite 主库已进入候选整合；尚未提交或推送。
- 配置边界：共享笔记、资源、用户/项目记忆、明确工具数据库及可移植 Obsidian 语义配置；工作区、缓存与 Excalidraw 临时工作区不共享。插件安装包和私有插件数据的最终边界留待单独规划。
- 已拆分项：25 个插件安装或机器私有数据文件的删除已撤出暂存区，首个共享快照不改变 Windows 既有插件文件；后续在双端备份、对位核查和凭据处置后再单独迁移。
- 链接审计已处置：仅包含 Excalidraw 临时图片节点的 `路径图工作candy.canvas` 与 `Excalidraw/` 一并改为本机忽略内容；插件使用说明已同步标注其不属于共享资料。
- SQLite 审计：主库纳入共享范围，但 Git 不支持二进制数据库的三方合并。每次提交前应关闭相关写入工具并确认 WAL 已回写主库；两台设备不得并行修改同一 `.db`，切换时先完成 pull → 修改 → commit/push 的单写者流程。

## 关联

- [[AI总MOC|AI 总 MOC]]
- [[02-PROJECTS/Agent/工作流/规范-任务产出入库与维护|任务产出入库与维护规范]]
