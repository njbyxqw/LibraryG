---
title: 2026-09-13 工作记录
date: 2026-09-13
type: daily
status: current
tags: [Daily, TileScape, LibraryG]
---

# 2026-09-13

## 今日索引

- 本次涉及项目：TileScape、LibraryG。
- 完成事项：继续 DDAV3 R02，追踪控制器按 TileType 评分到执行位置交换的调用链。
- 关键结论：同花色候选共享即时预测值，但具体候选被随机选中后会与触发目标互换位置、已见状态与序列成员关系；后续揭牌路径可能因位置/序列不同而分叉，尚未证明对指标产生错误。
- 产出文档：[[02-PROJECTS/TileScape/游戏逻辑/任务-DDAV3逻辑策略持续研究|DDAV3 持续研究主档]]。
- 来源版本：TileScape `DDA-V3-Test @ cd3aea16656814e0e226205c3c225c36a94deded`，与主档版本相同，无新增 DDA/Slack 提交。
- 验证边界：源码静态追踪；未运行 Unity、自动化牌局或测试。
- 后续入口：研究主档 R02，构造同花色不同位置/序列候选对照牌面。

## TileMatch

本轮未读取或修改 TileMatch，未形成该项目可入库结论。

## TileScape

### DDAV3 持续研究 R02（执行位置影响）

- 从 `InLevelDDAV2Strategy.TryExchangeTileInternal` 追至 `TileService.ExchangeTilePairsPosition`，确认控制器先按候选花色评价，再对具体合法同花色实体随机索引；执行时交换目标与所选实体的位置，并处理 `HasSeen`、序列成员关系。
- 结论限于预测粒度和执行路径：即时 HardSlack 不区分同花色实体的位置，但后续牌面状态可能分叉。需构造同花色不同深度/序列位置的合法对照，测量后续可见路线及指标；当前不定性为缺陷。
- 既有 Flip 序列辅助契约覆盖部分序列约束，但无完整 DDA 控制器综合选型用例。本轮只读测试代码，未运行。
- 已将来源方法和下一步样本要求补入研究主档；无需新增 MOC 入口。
- 另从 `ComputeMetrics` 公式代入确认，`HardSlack=-2` 同时表示“没有任何路线”与“存在路线但成本至少达到 `freeSlot+2` 的截断值”。因此该下限是过载桶，不是死局证明；候选安全边界也无法区分桶内两类状态。数值为静态公式推演，未构造棋盘或运行测试。

### DDAV3 持续研究 R03（回放配置快照首轮）

- 核对 `DDA-V3-Test @ cd3aea16656814e0e226205c3c225c36a94deded` 的录制入口、DDA 二进制配置负载和回放恢复分支。
- 确认记录会存 DDAType 及传统 V2 阶段/概率/道具限制配置，恢复时重建该 V2 配置并避免从当前 Jungle 活动状态重新计算覆盖项；Slack 开关及其曲线、死区、路线成本、步长参数仍是源码静态项，不在 DDA config snapshot 内。
- 影响边界：记录快照本身不足以识别 Slack 算法/参数版本；但记录另存 Game/BuildVersion，需继续核对其回放门禁。不能据此直接断言整条回放必然不一致。本轮未启动重连或回放。
- 产出：研究主档增加 R03 首轮和下一断点；后续追随机流、交换操作回放与实际交换统计口径。
- 进一步核对 `ShouldReplay` 与 `ShouldReconnect`：普通 Replay 校验关卡名/MD5（Game 模式还校验 LevelId），不比较 App/BuildVersion；回放 move 只重发点击、道具和状态输入，DDA 决策会由当前代码重新计算。Reconnect 则要求 record/app/build 版本一致。因此旧输入回放不保证还原历史 Slack 算法结果，断言限于静态调用路径；重连有更严格的构建门禁。
- 追踪随机流：`TileMatchGameLogic` 的 `RandomService` 是逻辑侧唯一随机流，DDA 候选平手选择与同花色实体随机选择均调用它；LogicRandomSeed 从记录恢复。建局完成实体/牌型初始化与 DDA 初始化后会执行 `ResetRandomService()`，所以初始化阶段的随机消费不会直接改变 DDA 起始随机位点。Replay/Reconnect 的 `ReconnectStrategy` 使用记录的 `TileRandomResult`，正常新局使用按 DDA 类型分流的牌型策略；这两条路径都在重置前完成。静态条件下，同构建、同种子、同输入仍有可重复基础；跨代码版本或新增随机消费者仍可能改变 DDA 结果，未运行回放对照。

### DDAV3 持续研究 R03（随机流与统计口径）

- 本轮用户要求继续，核对 HEAD 仍为 cd3aea166；完成初始化随机重置、TileRandomResult 恢复、局内 DDA/道具/Rocket 共用随机服务的静态追踪。
- 确认 Range(0,1) 仍消费随机状态；仅改变候选规则也可能改变后续共流结果，不能用相同种子单独证明版本可比或可重放。
- 确认 control_times/control_change_times 按调控批次累计；策略内部另有逐目标计数。统计进度使用 Board+Bar+OverBar，Slack 曲线进度只用 Board，不能直接对齐。
- 产出：[[02-PROJECTS/TileScape/游戏逻辑/任务-DDAV3逻辑策略持续研究#R03 第二轮：随机流、初始牌面与统计口径（2026-09-13）|R03 第二轮记录]]；更新 G5 状态与断点。未改代码，未运行 Unity/测试；后续回到 R02 场景矩阵和 R04 策略收敛。

### DDAV3 R04：场景反例与迭代选项

- 新增 [[02-PROJECTS/TileScape/游戏逻辑/分析-DDAV3场景反例与迭代选项-2026-09-13|场景矩阵子文档]]：10类指标场景、9类决策边界、6类体验对照，以及3条路线与分阶段退出条件。
- 推荐先补不交换基准和误差改善约束；道具/复活保护、可行性保护分别评估。建议未实施，数值例为模型代入，非Unity复现。
- 更新研究主档G2/G3/G4/G6状态并挂MOC；下一批R05核对真实点击—入槽—匹配时序与弃牌资格。源码版本cd3aea166未变，未运行测试。

### DDAV3 R05：入槽快照与弃牌可用性

- 已确认自动化06-00仍ACTIVE并绑定主档；保留原计划。
- AddToBar在Bar.AddTile更新逻辑占槽后触发DDA，再继续匹配相关流程；未匹配计数按花色取余，不等于实体数量。
- InOverBar且满足可见性/锁定/效果条件的牌可走取回入槽链；FastNCD仍排除OverBar，M07边界得到源码支持。
- 主档新增R05与R06断点；未改业务代码、未运行测试，未构造真实关卡。

### DDAV3 R06：配置、匹配事件与弃牌栈

- 普通Normal配置响应BarChanged执行匹配，图形/Headless在点击移动完成后发布该事件；此前已发生逻辑入槽和DDA评估。
- 新发现：点击BarHasSpace扣ReservedSpace，Slack空槽未扣；条件反例已记录，实际交叠待追，不认定为已复现缺陷。
- OverBar列顶y=0高亮，底层仅Visible；M07细分顶牌与压住牌，记录最小状态约束。
- 主档已新增R06和R07断点；未修改代码、未运行Unity/测试。

## LibraryG

- 按 `AGENTS.md` 与任务产出入库规范更新持续研究主档和本日 Daily，保留代码事实、推演及运行验证边界。
