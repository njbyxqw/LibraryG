# 2026-09-03

## Mac 端：知识库内容归属与 MOC 路由整理

- 执行「知识库内容归属与 MOC 路由整理」任务包（`02-PROJECTS/Agent/工作流/任务包-知识库内容归属与MOC路由整理-2026-09-03.md`）。
- 新建 `03-KNOWLEDGE/_MOC.md`（可迁移知识层入口）与 `02-PROJECTS/Agent/工作流/_MOC.md`（工作流/维护规范入口），并接入 AI 总 MOC 任务路由、TileMatch/TileScape 总 MOC 顶部任务路由表。
- 同步 `规范-MOC命名与导航层级.md`（补两个 MOC 登记 + "项目 MOC 优先"边界）与 `规范-任务产出入库与维护.md`（通用知识来源/范围/验证要求）。
- 未移动/删除/覆盖任何 `03-KNOWLEDGE` 历史文件，未 commit/push。`Unity.md` 现为空文件，与任务包描述（正文重复 MOC 规范）有出入，已登记 `needs-review`。
- 执行 P2「MOC 使用情景静态验证」：5 情景链路全部可达、无断点、裸文件名链接无歧义。发现 2 处误入风险（均 P1 待决）：通用知识 MOC 入口表含 2 条非可迁移条目；HOME.md:249 `[[03-KNOWLEDGE/Unity|Unity 速查]]` 指向空文件 vs 目录的歧义路径。验收报告 `00-INBOX/2026-09-03-知识库内容归属与MOC路由整理/验收-MOC使用情景验证.md`。

## Windows 端：LibraryG vault 建立私人 git 仓库

### 做了什么
1. 在本地 `D:\LibraryG` 初始化 git 仓库（main 分支）
2. 编写 `.gitignore`：排除 `.workbuddy/memory/`、`.smart-env/`、`.obsidian/workspace.json`、`*.db`、`.trash/`
3. 首次提交（353 文件，63.3 MB）
4. 配置远程 `git@github.com:njbyxqw/LibraryG.git`（私有仓库）
5. SSH 认证：复用本机 id_rsa 公钥（已由用户添加到 GitHub）
6. 远程已有自动生成的 `Initial commit`（README.md），用 `git pull --rebase` 合并后推送成功
7. 推送成功，本地与远程 main 一致（commit `ae41b0e`）

### 遇到的问题 ⚠️（已解决）
- `git pull --rebase` 过程中，`.workbuddy/memory/` 目录（untracked/gitignored）被意外清空
- 丢失：19 篇工作日志（2026-06-25 ~ 2026-08-28）+ 原 MEMORY.md

### 恢复 ✅（VSS 卷影副本）
- 发现 D 盘有 2026-09-01 20:52 的卷影副本快照（早于删除时间）
- 通过 `\\?\GLOBALROOT\Device\HarddiskVolumeShadowCopy40\LibraryG\.workbuddy\memory` 访问快照
- 20 个文件（19 篇日志 + MEMORY.md）全部完整恢复，大小/时间戳与删除前一致

### 结论
- LibraryG vault 已成功备份到私有 GitHub 仓库
- 后续每次改动后 `git add -A && git commit && git push` 即可同步
- **教训**：沙箱内 git rebase/checkout 会误删 untracked 文件，操作前先备份；误删后可用 VSS 卷影副本恢复

## Windows 端追加：拉取 Mac 端更新（14:44）

### 做了什么
1. 发现远程有 Mac 端（guoxiaolei）推的 3 个提交，用户要求「拉下最新的」
2. 用户决策：`.workbuddy/memory/` **纳入版本控制**（改变之前"不进 git"的规则，接受 Mac 做法）
3. 用 merge（非 rebase）拉取，避免重演误删
4. 修复 merge 中沙箱又误删的 64 个 TileMatch 文件（git restore）
5. 恢复 Excalidraw 70 图片 + candy.canvas 本地副本（Mac 端将其排除出 git，.gitignore 已含 Excalidraw/）

### 关键变化（Mac 端带来的）
- `02-PROJECTS/WorkBuddy/` 重组为 `02-PROJECTS/Agent/`（通用 Agent 工作空间定位）
- 新增 `.gitattributes` 统一换行符 LF
- `.gitignore` 移除 `.workbuddy/memory/` 排除、新增 `Excalidraw/` 排除
- 7 篇本地独有日志（08-05~08-28+今天）恢复为 untracked，按用户决策**暂不提交**

### 结论
- 本地 HEAD = 远程 = c94c182，已同步
- memory 已纳入版本控制（规则已更新）
